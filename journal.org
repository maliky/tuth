
* <2026-01-22 jeu. 15:09> Observations
What is this     grouped = defaultdict(list)


* <2026-01-21 mer. 22:08> Registrat UX + Invoice and Finance cleaning

  Registrar UX (grades list)

  - Query & search
      - Build a registrar grades view with select_related for student, section, course, faculty, semester.
      - Default semester filter to the current semester (reuse the existing resolver).
      - Search across student name/ID, course short name/code/title, and faculty name.
  - Grouping + pagination
      - Add a view mode control (dropdown or checkbox):
          - Student view: grouped by student
              - Summary: GPA, total grades, total credits
              - Within student: group by semester, then show section‑level grades
          - Course view: grouped by course
              - Summary: # students, # sections
              - If no semester selected: group by semester, then by section, then grades
          - Faculty view: grouped by faculty
              - Summary: # students, # courses, # sections
              - If no semester selected: group by semester, then section, then grades
      - Paginate 100 rows per page.
  - UI
      - Registrar dashboard panel should include:
          - Search field
          - Semester dropdown
          - View mode selector (student/course/faculty)
          - Collapsible grouped blocks (as above)
      - Remove any panels that don’t add meaningful signal.
  - Performance
      - Use select_related/prefetch_related to avoid N+1 queries.

  ———

  Admin invoice + finance cleanup

  - Course merge script
      - Add SQL script to merge courses with the same short_code.
      - Target selection: course with description, else smallest ID.
      - Repoint curriculum courses, prerequisites, sections, etc.
      - Optional follow‑up: wrap as admin action.
  - Invoice payments column
      - Add payments_count to InvoiceAdmin.list_display.
      - Use annotate(Count("payments")).
      - Make the count link to the payment admin changelist filtered by invoice.

  ———

  Student dashboard refactors

  1. Move _section_fee

  - Prefer existing Section method if present; otherwise add Section.fee_total().
  - Update all callsites to use the model method.

  2. Move datetime formatters

  - Add format_datetime and format_short_datetime to app/timetable/utils.py.
  - Replace local helpers in student_dashboard.py with imports.

  3. Sidebar context

  - Consolidate sidebar context in a shared helper or context processor.
  - Keep per‑view custom active state only if required.
  - Remove any unused cards or panels in the student UI.

* <2026-01-21 mer. 22:00>  Registrar UX
The registrar dashboard shoudl show the list of grade paged by 100 groupe by students with searchable field per student, course shortnaem long name, and faculty.  filter shoudl be availaabe for the semteser. current semester should be the default.


* <2026-01-21 mer. 21:15>  Admin invoice and finance
- Make a sql script to merge together all courses that share the same short_code.  The default target should be the one with a course  description, or the one with the smalest id.  If cleaner do this as an admin action.
- in admin invoice Add a payment column showin gthe number of payment related to that invoice.  clikcking the number should open the payement admin list filtered down to those payements.
  
- in student_dashboard.py
  - move _section_fee to Section. make it a class method
  - move format datetime, and format_short_datetime  in app/timetable/utils.py
  - I am surprised to see us rebuild the context for the side bar in student_invoice_Statement, I though this side bar would be inherited through template logics.  am I missing something essential ?
* DONE <2026-01-21 mer. 20:55> Finance officer UX
CLOSED: [2026-01-21 mer. 21:35]
- need more structure in the list.  When showing several student the studnet invoice details should be hidden (folder) under a summary row with the student name (ID) and total amount due.  A button  to create payments for this user should be on the row of thuser.  when presing it, it create all the invoice's payment for that users.  The genera button create payment should be called create indiviudal payment, and only show when a row is specificaly selected.
- The tab display is good.
- On the Payments tab.  Same folded display per listed student.
- Amount paid should have de default value but it should be editable.
- Status and Method should be editable also (drop down lists).
- A 'validate change' button should be display at the student row level.  pressing it will push to the db the update on the payment rows, save them and update the invoice amount du and change their state if amount due drp below 0

* DONE <2026-01-21 mer. 15:57>
CLOSED: [2026-01-21 mer. 20:54]
Key payment logic note: invoice balances only change when a payment is saved with status cleared in app/finance/models/payment.py, so creating a payment alone does not reduce amount_due.

  - Student portal CTA
    - Rename button label to “Proceed to Payment”.
    - No payment creation on this action; it just redirects to the statement.
    - Invoices continue to be created when a student saves registrations.
  - Invoice statement behavior
    - Statement rows come from invoices with amount_due > 0, not registrations.
    - Remove the “Updated” column; show only invoice created_at
    - Exclude canceled/removed registrations by deleting their invoices and any related payments on cancel. (no soft delete for now)
    - Sidebar link remains as-is.
  - Student course list UX
    - Remove the “Registration status: …” line.
    - Canceled badge text becomes:
      - “Canceled for semester” if no unlock timestamp.
      - “Canceled until Jan 23, 15:42” using a short %b %d, %H:%M format.
    - Keep canceled courses in the visible registered list.
    - Hide the “Credits selected” row in the cart when no sections are selected (front-end only).
  - Admin invoice list
    - Update list_display to include student name (ID), semester, amount_due, created_at.
    - Add filters for semesters, amounts due  (if  simple to implement )
    - Add list_select_related for student/user/semester.
    - Search by Student name and ID and curriculum course
  - Admin payment
    - Hide recorded_by in the form; default to the logged-in user on save.
  - Admin “Create payment” action
    - Bulk action that also accepts a single invoice.
    - Uses a shared helper to create pending payments (idempotent, skip if pending exists or amount_due <= 0).
      - In the bulk action view for eah payment created set the default amount paid to the full invoice payment, but let the finance officer adjuste as necessary
  - Finance officer UI
    - Add a dedicated invoice/payment view with tabs:
      - Invoices tab: last 100 with amount_due > 0, ordered by created_at desc.
      - Payments tab: last 100 emphasize pending payments to clear.
    - Student dropdown limited to students with invoices or payments.
    - When a student is selected, show all his invoices/payments regardless of status.
    - Status filters for payment per tab.
    - Bulk “Create payment” action in the invoice tab using the shared helper.
    - Pagination controls.

  Assumptions to confirm

  - Deleting payments on registration cancellation is required even though it removes payment history. No soft delte for now. The ide is tha a registration cannot be deleted if a payment has been made and the status is not pending anymore. There is no refund policy implemented yet.


* DONE <2026-01-21 mer. 15:27> Invoices and payments
CLOSED: [2026-01-21 mer. 22:06]

  Updated Plan – Invoices & Payments (15:18)

** Student portal: “Proceed to Payment” flow

  - Rename button label from “Download statement” to “Proceed to Payment”.
  - On click: create a Payment row per invoice (not a new invoice) for all outstanding invoices, then redirect to the statement view.
    - Yes invoices are created when the student save the registration. He first save teh registation than proceed to payment
  - Ensure the created payments are pending (finance clears payments, not invoices).

** Invoice statement behavior

  - What appears in the invoice statement are invoices with amount_due > 0, not registration.
  - Canceled registration must also destroy the associated invoice and associated payment(s).
  - Statement shows only non‑canceled registrations; filter canceled/removed registrations out of the invoice list.
  - Remove the “Updated” column; keep only “Registered” (created timestamp).
  - Sidebar link label remains in the student portal, but the CTA/button is “Proceed to Payment”.

** Student course list UX

  - Remove the “Registration status: …” line and replace the badge label for canceled rows:
    - “Canceled for semester” when no unlock time.
    - “Canceled until Jan 23, 2026, 15:…” when the unlock time exists.
  - Keep canceled courses in the visible registered list (not collapsible).
  - Hide the “Credits selected” row when no sections are selected in the registration cart (front‑end only).

** Admin – finance/invoice list enhancements

  - Update InvoiceAdmin list_display to include student name+ID, semester, amount_due, created_at.
  - Add list_select_related to avoid N+1 (student/user/semester).
  - Add a “Create payment” action (or per‑row button) that uses a shared helper to create pending payments and optionally mark them as cleared per rules.

  - In admin/finance/payment

    - hide the recorded_by. It defaults to the logged in user.

** Finance officer dashboard (staff UI)

  - Add a dedicated finance officer invoice/payment list view + template:
    - Default to last 100 invoices with amount_due > 0, ordered by created_at desc.
    - Search by student name or student_id.
    - Group by student; when a student is selected, show all invoices or payment what ever the status
      - Have dropdown box to filter down payement/invoices by status
      - In the dropdown list only student with payments or invoice should be proposed.
    - Show payments alongside invoices but emphasize payments to be cleared.
      - This should be 2 different tabs of the same UI
    - Add per‑row “Create payment” action that uses the shared helper and similare logic as in the admin.  select row, selection action , validate.
    - Add pagination controls.

** Shared helper for payment creation

  - Create a single helper that accepts invoice(s) and creates Payment rows (pending by default).
  - Keep payment creation idempotent (avoid duplicates if a pending payment already exists).

  If this looks right, confirm whether the “Create payment” action should also be available for a selected list in admin (bulk
  
* DONE <2026-01-21 mer. 15:18> Invoices and payments
CLOSED: [2026-01-21 mer. 15:27]

-  Rename the buton 'download statement' with Proceed to Payment'.  This will show the invoice statement to be printed and brought to finance with payment proof.
- important change, when one clicks on proceed to payment, a payement is created for each invoice.  It is the payment that the finance clear, not the invoice direclty. The invoice are cleardd depending on rules related to the amount due.
  Still on the finance ui, the officer should see both the invoce and the payements but emphasize is on the payements to be cleared.   

** DONE <2026-01-21 12:50> Student portal
CLOSED: [2026-01-21 mer. 22:06]

  - Sidebar “Download statement” link + consistent sidebar on statement page: OK
  - Canceled registration status + gray display + locked‑until timestamp: Partialy
    - remove the ligne with 'Registration status' make badge a little more informative when necessary:
      - Canceled for semester
      - Canceled until Jan 23, 2026, 15...
  - Statement shows created/updated timestamps: partialy
    - Remove the updated column.  and make sure canceled registration do not show in the invoice statement.  Currently they somme show.

  - Credit limit shows remaining credits: compliant;  do not show ' <div class="d-flex justify-content-between">
            <span class="text-muted">Credits selected</span>
            <strong><span data-credit-selected="">6</span></strong>
          </div>' if not section selecte in the registration card.



**  <2026-01-21 13:02> Finance admin + finance officer

  - Admin invoice list (student name/ID, semester, amount due, created time): not compliant; InvoiceAdmin still only
    shows __str__ and recorded_by in app/finance/admin/core.py.
  - Finance officer view with invoices (amount_due > 0), grouped by student, search by name/ID, pagination, and per‑row “create payment”: not implemented; finance officer uses the generic dashboard actions in app/website/views/staff_dashboards.py and no invoice list exists in staff templates.
  - Admin “create payment” action: not present; no action/button in app/finance/admin/core.py.
  - Note: invoice balance only updates when a payment is saved with status=cleared (see app/finance/models/payment.py), so any “create payment” button must set that status to update amount_due as requested.

Add a “Create payment” action in the admin invoice list and reuse the same helper in the finance officer UI for consistent balance updates. The staff view can mirror the admin list, but it needs its own  view/template (grouped by student, search, pagination, and per‑row POST action).




* DONE <2026-01-21 mer. 12:21>
CLOSED: [2026-01-21 mer. 15:24]
1. Generate the invoices and the statement only on the "Dowload statement" action.
2. On click : gather pending registrations for the current student but for any semester, create missing Invoice rows if they don't exists; reuse existing ones.
3. Statemetn page aggregates all invoice row for the student, showing only rows with amount_due > 0.
4. Button appear only when there are pending registrations for the current student.
5. Add a registration guard to prevent same course in multiple sections for the same semester.
6. No new Invoice model required.
   Do not do the DB test now.

   Implement the above.

** DONE <2026-01-21 mer. 10:42>
CLOSED: [2026-01-21 mer. 12:21]

Review this   plan to Implement 

  1. Define the “invoice statement” concept without a new model (initially).
      - Create invoices only when the student clicks “Proceed to payment”.
      - Use the existing Invoice rows as line items (one per course per semester).
      - The rendered invoice page aggregates those rows into a single statement (total of all courses).
  2. Add a server‑side “generate invoice” endpoint.
     # > this is not clear. The invoice should only be created one for the pending regsitration for student in a semester.
     # > Then if there are already pending invoice, for that student, they should also display and be comgined in one invoice.
      - New view in app/website/views (or a dedicated finance view module):
          - POST only (since it writes to DB).
          - Collect pending registrations for the selected semester.
          - For each registration: compute the fee (tuition + section fees) and get_or_create the corresponding Invoice.
          - Keep it idempotent so repeated clicks don’t create duplicates.
      - Redirect to a GET invoice display URL after creation.
  3. Add invoice display endpoint + printable template.
      - New URL in app/website/urls.py (e.g., /student/invoice/<semester_id>/).
      - New template (e.g., app/website/templates/website/student_invoice.html) with:
          - A printable layout (CSS @media print).
          - Header, student info, term, line items (course, credit, fees), totals.
          - A small “Print / Save as PDF” instruction (browser print dialog).
  4. Wire the button on the dashboard.
      - Replace or reuse the existing “Proceed to payment” button in app/website/templates/website/
        student_dashboard.html.
      - Make it a POST form that hits the “generate invoice” endpoint.
      - Show it only if there are pending registrations for the selected semester.
  5. Only  pending registrations for the active/selected semester are included
  6. Optional evolution (if you want a real “invoice document” object later).
     # > don t  I have an invoice object already ?  I think that together with payemnt it should be enough.
      - Add an InvoiceBatch or InvoiceStatement model to group the line‑items and keep a stable invoice ID.
      - Link existing Invoice rows to the batch via FK for historical re‑render.

My observations:
  # - Any created  invoice that is not cleared should show ?
  # - A student should not be able to register in multiple sections of the same course. The Registratin unique constraint should guarde agasint that. if not needs to be guarded

  DB‑Only Test Plan (No UX)

  - Create registrations (pending) and verify no invoices exist until the “generate invoice” view is called.
  - Call the “generate invoice” view and verify:
      - It creates one Invoice per course per semester.
      - It is idempotent
      - Total sum equals tuition + section fees from SectionFee.
  - Verify filtering:
      -  Only registrations pending are included


* <2026-01-20 Tue 20:52>
What is candidate_map in "selected_obj = form.candidate_map.get(str(selected_id))"
Make sure to add typing hint in function signature
Do not write comments stating with '# >' yourself. It is the second time I am telling you this. how can I make clearer so you do not do it again ?
Harmonize test types aliases. make sure they finish with T.

* DONE <2026-01-20 Tue 15:02>
CLOSED: [2026-01-21 mer. 22:07]
  Create Semester.get_default()  -> current semester
  Create AcademicYear.get_default()  -> The default semester of the current semester
  Wrap the above in test fixtures and reuse fixtures for sections if any, else create them. idem for CreditHours.

  Test 1:
  I allow duplicate curriculum or course but not the pair (curriculum, course). Check if you identified something pointing in another direction.

  Test 2:
  Do not allow merging of different Course. no allow_course_override, remove the test and guarde this type of merge.

  Test 3:
  Create a fixture for the invoice.  Should not be a problem since We do not have invoice in the DB.

  Test4:   OK
  # > ignore comment below
  # Once I've selected the 2 curriculum and the merge curriculum action in the admin and press ok,  I want a form offering to select wich curriculum field will be taking precedences. so I should be show the course conflincting field to select which to merge. after selection, the merge should take place ...  
  Test 5: ok
  Extensions A, B OK
  C: how can I have same curriculum/course ? with
  '''    class Meta:
        constraints = [
            models.UniqueConstraint(
                fields=("curriculum", "course"), name="uniq_course_per_curriculum"
            )
        ]
''' or '/' means 'or' as in same curriculum or course ?

  
** Graphic color on links or code dashed, double ect... to better identify them
* DONE <2026-01-20 mar. 10:54> Merges
CLOSED: [2026-01-21 mer. 22:07]
Here is the analysis and a concrete improvement plan, based on the current code paths (no code changes made).
 
  Current Merge Mechanisms
 
  - app/academics/admin/merges.py implements merge logic for departments, curricula, courses, and curriculum courses.
    - Course/department use simple admin actions;
    - curriculum/curriculumcourse have deeper merge logic.
  - app/academics/admin/core.py wires the actions.
    - Curriculum and CurriculumCourse use MergeWizardMixin;
    - Course and Department use direct actions.
  - app/people/admin/mixins.py provides MergeWizardMixin;
    - app/people/admin/core.py uses it for User, Faculty, Staff, Student, Donor. Actual merging is done by app/people/services/merge_people.py.
  - app/timetable/models/section.py and app/timetable/models/session.py define the Section/SecSession constraints, but there is no admin merge action for them. The only section merge logic is _merge_sections in app/academics/admin/merges.py.
  - scripts/curriculum_merge_conflicts.sql is the existing SQL pre-check for curriculum merges.
 
  Culprits / Risks Found
 
  - Course merge action is broken: merge_courses tries to update a related field (curriculum_course__course), which is invalid and causes the error you saw. It also risks losing sections via cascade when the source course is deleted. app/academics/admin/merges.py.

  - Department merge is too narrow: it only updates Course.department via .update(), which skips Course.save() and leaves code/short_code inconsistent with the new department. It also ignores other Department FKs (Staff,  RoleAssignment), so it can leave data pointing to deleted departments. app/academics/admin/merges.py, app/ academics/models/course.py, app/people/models/staffs.py, app/people/models/role_assignment.py.
  - Curriculum merge uses _merge_curriculum_course_to_target and _merge_sections correctly, but it stops the entire merge on a single invoice conflict and does not resolve credit hour conflicts. app/academics/admin/merges.py, scripts/curriculum_merge_conflicts.sql.
  - CurriculumCourse merge allows any two rows with the same curriculum and same course department, even if the course differs. That can merge unrelated courses. It also does not detect pre‑existing duplicates outside the selection. app/academics/admin/merges.py.
  - People merges are risky: merge_people does not reassign related records (registrations, grades, invoices, scholarships, documents, payments recorded_by). This can delete data or raise PROTECT errors when the source is  deleted. app/people/services/merge_people.py, app/registry/models/registration.py, app/registry/models/grade.py, app/finance/models/invoice.py, app/finance/models/scholarship.py, app/registry/models/document.py, app/finance/ models/payment.py.
  - merge_users updates RoleAssignment in bulk; if target already has the same role assignment, the unique constraint can fail. app/people/services/merge_people.py, app/people/models/role_assignment.py.
  - Section/SecSession have no direct merge action; _merge_sections only merges sessions/grades/registrations and ignores section fields (faculty, dates, info, seats). app/academics/admin/merges.py, app/timetable/models/ section.py, app/timetable/models/session.py.

 Plan to Correct and Improve
 
  1. Pre‑merge conflict checks (SQL‑first, as requested).
      - Keep using scripts/curriculum_merge_conflicts.sql for curriculum.
      - Add similar SQL scripts for course, curriculumcourse, and department merges to surface:
          - course duplicates per department, curriculumcourse duplicates, section collisions, prerequisite collisions
          - staff/role assignments pointing to departments
          - student/grade/registration/invoice collisions for people merges
  2. Fix course merge logic to reuse existing helpers instead of updating across relations.
      - For each source course, iterate curriculum courses:
          - if target already has the same curriculum course, call the existing curriculum course merge helper (reuse _merge_curriculum_course_to_target and _merge_sections)
          - otherwise move the curriculum course to the target and save
      # - Update prerequisites that reference the source course (both course and prerequisite_course) with dedupe checks.  
      - Only delete the source course after all dependent objects are moved.
  3. Improve department merge.
      # - Reassign all Department FKs (courses, staff profiles, role assignments) to target. ?
      # > Department only FK is college.
      - Recompute and save Course.code/Course.short_code after moving departments to prevent stale codes and uniqueness issues.
	# > Course code are ok for now
      - Provide a summary of collisions detected by the SQL pre‑check before proceeding.
  4. Tighten curriculumcourse merge rules.
      - Require same curriculum and same course unless the user explicitly selects a different course in the wizard, then validate for collisions.
      - Add conflict summaries for credit hours and flags; do not silently pick one.
  5. Make curriculum merge more resilient.
      - Use the SQL script before merging.
      - Change invoice conflicts from “raise and stop everything” to “skip with summary,” matching the curriculumcourse merge behavior.
      - Provide a summary for credit hour differences (no auto overwrite).
  6. Harden people merges by reassigning dependent records.
      - Student: move registrations, grades, invoices, scholarships, scholarship snapshots, and documents before deleting source.
      - Staff: move staff documents and reassign recorded_by where appropriate.
      - Donor: move donor documents, scholarships, and letter templates.
      - Update merge_users to dedupe RoleAssignment instead of bulk update to avoid unique constraint collisions.
  # 7. Decide whether to add direct merge actions for Section and SecSession.
  #     - If yes, use MergeWizardMixin and reuse _merge_sections for the dependent records.   
  #     - Add field‑level merge handling for section fields (faculty, dates, info, max_seats).
  # > not for now.
  8. Keep all changes functional and reuse existing helpers; avoid alias wrappers.
      - Add or update Google‑style docstrings and inline comments for any touched functions.

  If you want me to implement, tell me which model group to start with (courses, departments, people, or curriculum/
  curriculumcourse), and whether you want the SQL conflict scripts added first.

    

* <2026-01-19 lun. 00:36>
- propose a test for import grades and import_sessions if not yet implemented

* DONE <2026-01-19 lun. 00:07>
  CLOSED: [2026-01-19 lun. 00:36]
- in for eg /admin/people/student/1990/change/,
  - in inlineRegistrations, need to be able to remove a registration.
- I did an import through import_grade and I get many faculty without name just staff id and a username which is the staffid number +1.  Need to be investigaged
** student dashboard
- Do not display courses not sheduled this semester.
- Only display in gray courses that are pending payment or that are locked after cancelation
- in the Registration cart the credit selected should update as we add courses. now the credit selected is blocked at 0.
  - There should be a warning and a lock if the number of credit selected is more that the authorized amount for the student.  Identify where this number is set for a student.  It should be editable by a staff, stored in the student's model. It is something that is been updated every semester.
  - Give detail of what happen at the code level when I press 'Save registration'
*** WORK said to be done
• Updated the student dashboard registration flow to enforce per‑student credit limits, hide unscheduled courses, and refresh the “Current and past courses” table (plus the course list) via AJAX so it updates immediately after register/cancel. Added the per‑student credit limit field for staff to edit and wired the cart UI to show/lock on limit exceed.


  Import‑grade faculty issue investigation:

  - import_grades only uses Faculty.get_default() and does not create per‑row faculty records.  The likely source of numeric/blank‑name faculty is the name‑based creation in app/shared/management/commands/import_schedule.py or app/timetable/management/commands/import_sessions.py (both call ensure_faculty). If you want, I can add a guard there to skip or normalize rows with missing/non‑name faculty fields.

  Next steps you may want:

  2. Verify the AJAX flow by selecting a section, saving registration, and canceling to confirm the table and course list update immediately. NOT WORKING
  3. If you want, I can harden ensure_faculty usage in the import commands to avoid creating  blank faculty records.
    
    
* DONE <2026-01-18 dim. 22:52> Registration/reservation workflow
  CLOSED: [2026-01-18 dim. 23:50]
  - Think of where to block the multiple registrations of a student for a curriculum course in the same semester. I want to give him 2 chances separated by 48h. this Sould be computed on the fly, looking at the registration table history. no stored field, jsut computed values. check that the current workflow is compliant with this propoistion.
  - Not sure about the naming of reservation.  undo the previous changes.  lets have registration every where (code, comments included), the distinction will be the status.
** local install js
- download the all js files, eg bootstrap. reduce dependency to excternal js library files.
   
** Student dashboard
- When clicking cancel in Current and past courses, nothing is happening. I expect the line to disapear from the current and past course.
- the already reserved / registration pending courses should not be mixed with courses not offered this semester.  They should be displayed alongside the other  course reservation but grayed and locked
- When clicking 'Save reservation' the reservation cart rest but the registration line with pending payment does not appear in Current and past courses as i expect. invetigate why.  I shouldnt need to relaod the page.
- the semester in Course and Grades section are still not ordered from the most recent to the oldest. Indicate where the odering is been done so I look at it directly.
- Add the time to the udpated in Current and past courses so the student can compute when before what time he has to pay.
  
** Refactoring
- Weekday parsing logic duplicated acrosse 2 import commands
- Time parsing logic duplicated with identical formats
- location splitting logic duplicated (include fall back in shared version)
- college/departments code normaliztion repeated in admin widgets and ensures
- academic-year normalization redundant.  Some function are alias or wrapper on others.
- Extract a small pure helper like _coerce_int(value: str | int) -> int | None that returns None on failure, and have: parse_int call it directly, and to_int wrap it and apply default when None.
     
** Worked
• Reverted wording back to registration across the student dashboard, moved the cooldown/attempt blocking to rely solely on registration history, and separated already‑registered courses into their own locked list so they aren’t mixed with “not offered” courses. I also centralized the weekday/time/location parsing, normalized college/department code helpers, removed the redundant academic‑year alias, and added _coerce_int to unify int parsing.

  Ordering note:

  - The Course & Grades semester ordering is done in app/website/views/student_dashboard.py in the semester_sort_info build + semester_grade_groups.sort(...) block (now uses semester start date or academic year start date).

  Local JS note:

  - I didn’t find any external JS references; bootstrap JS is already local. The only remote assets are CSS (Google Fonts in app/website/templates/website/landing.html and bootstrap‑icons in app/website/templates/website/staff/base.html).
   

* <2026-01-05 lun. 07:33>  Runing a sql cmd
docker-compose -f docker-compose-preprod.yml exec -T db psql -U tusis_preprod -d tusis_preprod -c "SELECT version();"


* <2026-01-05 lun. 07:19>
- Make test for NameParts if I have empty parts.

* <2026-01-06 mar. 18:00> Notes on seed_full_student_grades.py
- Target reader: intermediate Python/pandas engineer; comfortable with typing, argparse, set ops.
- Typical US comp for that level: ~$110k–$150k base; higher with equity/bonus in SF/NYC/Seattle, closer to ~$100k–$120k in lower-cost areas.
- To increase abstraction difficulty: introduce pipeline composition/IoC for loaders+savers, plugin registry for scoring strategies, structured logging/metrics with failure policies, config-driven IO/validation, and immutable data models with strict mypy coverage.
