• Source is two separate issues in the SecSession import path:

  - “Invalid row” comes from asserts_keys in app/shared/utils.py, called by SectionWidget.clean in app/timetable/admin/section_widgets.py. That widget requires
    curriculum, course_no, dept_code, college_code, and faculty. Your row has course_dept and no faculty, so the key check fails before any DB work.
  - Duplicate key on uniq_section_per_curriculum_course is from ensure_section in app/timetable/ensures.py. It uses get_or_create with semester, curriculum_course,
    number, and faculty_id, but the DB unique constraint in app/timetable/models/section.py is only on (semester, curriculum_course, number). If the section already
    exists, the extra faculty_id in the lookup makes get_or_create try to insert, which violates the unique constraint.

  Fix direction:

  1. Normalize SecSession rows in the resource (likely app/timetable/admin/session_resources.py) to supply dept_code from course_dept, semester_no from semester, and a
     faculty key (even blank).
  2. Adjust ensure_section (or SectionWidget) to look up by the unique fields only, then update faculty if needed—don’t include faculty_id in the get_or_create lookup
     unless the unique constraint also includes it.

# ##

- Need to centralize the place where I list header and header translation map for different tables.

- Check the test they can be cleaned up, many are redondent.

- faire les test pour import section and grades

* <2026-01-05 lun. 09:51> UI
- The bio field does not show in faculty

* <2026-01-05 lun. 07:33>  Runing a sql cmd
docker-compose -f docker-compose-preprod.yml exec -T db psql -U tusis_preprod -d tusis_preprod -c "SELECT version();"


* <2026-01-05 lun. 07:19>
- Make test for NameParts if I have empty parts.

* <2026-01-06 mar. 18:00> Notes on seed_full_student_grades.py
- Target reader: intermediate Python/pandas engineer; comfortable with typing, argparse, set ops.
- Typical US comp for that level: ~$110k–$150k base; higher with equity/bonus in SF/NYC/Seattle, closer to ~$100k–$120k in lower-cost areas.
- To increase abstraction difficulty: introduce pipeline composition/IoC for loaders+savers, plugin registry for scoring strategies, structured logging/metrics with failure policies, config-driven IO/validation, and immutable data models with strict mypy coverage.
