
* creat a factory to generate these function
        def _create_faculty() -> Faculty:
            faculty, _ = Faculty.objects.get_or_create(username=username, defaults=_d)
            _pwd = default_password(_d["first_name"], _d["last_name"])
            faculty.staff_profile.user.set_password(_pwd)
            faculty.staff_profile.user.save(update_fields=["password"])

            return cast(Faculty, faculty)

        def _create_student() -> Student:
            student, _ = Student.objects.get_or_create(
                username=username, defaults=_d,
            )
            student.user.set_password(default_password(_d['first'], _d['last']))
            student.user.save(update_fields=["password"])
            return cast(Student, student)
        def _create_staff() -> Staff:
            staff, _ = Staff.objects.get_or_create(username=username, defaults=_d)
            staff.user.set_password(default_password(_first, _last))
            staff.user.save(update_fields=["password"])
            return cast(Staff, staff)

        def _create_person(username, Person, _d, user_getter) -> PersonT:
            pers, _ = Person.objects.get_or_create(username=username, defaults=_d)
            _user = user_getter(pers)
            _user.set_password(default_password(_d['first'], _d['last']))
            _user.save(update_fields=["password"])
            return cast(Person, pers)
            
  

* <2026-01-05 lun. 09:51> UI
- The bio field does not show in faculty


* <2025-12-30 mar. 07:18> improvements
- Consolidate duplicated “ensure” helpers (semester, user creation) and normalize type handling around get_user_model to avoid repeated casts/mypy friction.
- Reduce per-command manual deduplication caches by pushing merge/update logic into reusable services or managers; add tests to guard merge behaviors (e.g., ID, complementary rows).
- Tighten typing around Django models (use TypeAlias for user model, avoid dict[...] where Mapping fits) and prefer TypedDict/dataclass for row structures    to appease mypy.
- Align username generation across widgets/commands to prevent divergent suffixing rules; centralize in one helper with collision policy.
- Expand logging/reporting consistency: standardize CSV log paths/messages and include counts for skipped/merged rows; consider structured logs for import commands.


* <2025-12-29 Mon 09:32>  Looking into factorizing the fuzzy creations
 Implemented the exact-lookup path for Grade imports:
    - CourseWidget and CurriculumWidget now accept allow_fuzzy (default True);
  when False they perform exact lookups/creates without fuzzy matching and still
  update titles.
    - CurriculumCourseWidget and SectionWidget pass the allow_fuzzy flag down so
  you can toggle behavior.
     - GradeResource now uses SectionWidget(allow_fuzzy=False), bypassing fuzzy
  course/section matching for grade imports.
  Files touched: app/academics/admin/widgets.py, app/timetable/admin/widgets/
  section.py, app/registry/admin/resources.py.

  This should speed up the 193K grade import by using exact lookups for course/
  section while keeping fuzzy matching available elsewhere.
  ::: the above is implemeted but and it works well but I want to factor the
  get_or_create_fuzzy into the generic get_or_create for the Course and Curriculum
  Manager.  The idea is to pass a boolean (fuzzy) to the get_or_create of thoses 2
  models (and even for the people) if the boolean is true, use the fuzzy matching,
  and fall back to default if boolean is false.  First step is to check all the
  files that needs to be touch. do an extensive analysis of the code base then
  propose you action plan but do not implement it yet.  I want to review.

** proposition
I traced the current fuzzy flow and the spots we’d need to touch.

  What’s in place now

  - Course/Curriculum managers each expose find_fuzzy_match + get_or_create_fuzzy;
    regular get_or_create is untouched.
  - Only call sites for the fuzzy helpers are in app/academics/admin/widgets.py
    (CourseWidget/CurriculumWidget); allow_fuzzy=False paths use plain
    get_or_create.
  - Grade imports: GradeResource uses SectionWidget(allow_fuzzy=False) →
    CurriculumCourseWidget/CourseWidget/CurriculumWidget accordingly.
  - Person side: PersonManager has fuzzy name dedupe inside _find_by_name, used by
    create/update_or_create; get_or_create is still Django default. Staff/Faculty/
    Student widgets and forms call .get_or_create on those models (staff, faculty,
    student) without an explicit fuzzy toggle.

  Files likely to change

  - app/academics/models/course.py (CourseManager)
  - app/academics/models/curriculum.py (CurriculumManager)
  - app/academics/admin/widgets.py (switch to the new get_or_create(..., fuzzy=...)    API)
  - app/timetable/admin/widgets/section.py (propagates allow_fuzzy via
    CurriculumCourseWidget/CourseWidget/CurriculumWidget)
  - app/registry/admin/resources.py (constructor wiring already passes allow_fuzzy)  - app/people/models/object_manager.py (PersonManager fuzzy toggle)
  - Call sites that rely on PersonManager’s create/get_or_create/update_or_create:
    app/people/admin/widgets.py, app/people/forms/faculty.py, app/people/models/
    faculty.py, any other staff/student creation helpers.

  Proposed plan

  1. Extend CourseManager/CurriculumManager.get_or_create to accept a fuzzy flag
     (default False to preserve current behavior), reuse existing find_fuzzy_match
     logic, and keep get_or_create_fuzzy as a thin alias for backward
     compatibility.
  2. Update widgets to drop direct get_or_create_fuzzy calls and instead pass
     fuzzy=self.allow_fuzzy into the unified get_or_create; keep the title-update
     behavior for non-fuzzy Course creation.
  3. For PersonManager, add a fuzzy toggle to create/update_or_create/get_or_create     (and underlying _find_by_name) so callers can opt out of name-based dedupe;
     thread the flag through Staff/Faculty/Student creation helpers where
     appropriate.
  4. Audit secondary call sites (import_schedule command, fixtures/tests, staff/
     faculty forms/widgets) to ensure they either rely on default strict behavior
     or pass fuzzy=True explicitly where desired.
  5. Add/adjust tests to cover fuzzy vs strict paths for Course/Curriculum managers     and one PersonManager path, and update docstrings/comments to point to the new     fuzzy parameter.



*** <2025-12-29 lun. 13:17>
1. is ok, the cache will help but be precise with the type signature. it is an area where mypy is quick to complain.
2. I do not want allow_fuzzy boolean anywhere so remove it if still implemented (it should not) same as get_or_create_fuzzy, should have been removed totaly from the code for Course and Curriculum.
3. ok, agree
4. ok
5. ok
6. yes

you can proceed and make this code base grow beautifully.   



*** Head of  Seed_data/Fundamentals/full_grades.tsv
academic_year	semester_no	student_id	course_dept	course_no	section_no	grade_code	credit_hours	curriculum	college_code
2014/2015	2	13425	MATH	101	1.0	C	3.0	BBA - Banking & Finance	coba
2013/2014	2	921	HIST	102	1.0	C	3.0	BA - 2ndEd/Chemistry	coed
2012/2013	1	TU-00002	EENG	314	1.0	B	3.0	BS - Mech Eng	coet

this is the file imported.  I am still facing timing issues.  we start the import at 1.4s /iteration like previously this may increase at 2% of the file we were at 10s/it  but the cache should make the loop timing more stable and close to 2s/it.

the objective is to reduce the time below the second for each iteration.

Analyse in detail the code base and propose a plan to significatly decrease the time taken for a iteration.

*** 

docker-compose -f docker-compose-preprod.yml run --rm web python manage.py import_grade -f Seed_data/Fundamentals/full_grades.tsv  --batch-size 5000


* <2025-12-18 jeu. 21:49> Terms for students
  Define a rule to set the terms form the studentinfo import
  something like for oldest for  first TermFirstEntered and newest for TermLastEnrolled

* <2025-12-17 mer. 17:53>  fuzzy matching for curriculums
** You did well but a few observation
- do not abuse of .strip()  I take care of cleaning the data before reaching this deep in the code.
- We should be improve the factoring and code  readability (it's already good but...)
  - in curriculum find_fuzzy_match: the sequence in case of tie, could be factored in a function...
  - The idea is to reuse

- Do not forget to add documentation even if succint
-
** I want to refactor the usage of name_similarity
See in this files
app/people/admin/core.py
31:from app.people.matching import name_similarity
79:            score = name_similarity(base_name, other_name)
app/people/admin/mixins.py
10:from app.people.matching import name_similarity
78:            score = name_similarity(base_name, other_name)
app/people/matching.py
42:def name_similarity(
85:        score = name_similarity(base, name)
app/people/models/object_manager.py
 9:from app.people.matching import name_similarity
68:            score = name_similarity(base_name, candidate_name)

We use a very similar algoritm.
I want to factor it using a something like below:
    # create the base name (tokenA)
    # get candidates
    # initialize storage
    # Loop on the candidates
    #  for each candidate C create the  a comparaison key (tokenB)
    #  score tokenA -- TokenB distance
    #  update storage based of threshold
    # return

    # vars, a function to get candidates or the candidate them selves
    # a function to get tokenA or tokenA itself
    # a function f to get the tokenB : f(C,*args) -> tokenB
    # thresholds to update storage

or even using the already defined function find_similar_names.

Do your analysis on how we can make our code sweeter.

* <2025-12-15 lun. 23:53>  running the tests ?
  working
  need to
    Tusis_app $ sudo -u postgres psql -d postgres
    postgres=# grant create on database postgres to tusisuser;
    sudo -u postgres psql -c "ALTER ROLE tusisuser CREATEDB;"



* TODO <2025-11-11 Tue 17:09> View routing map

 CLOSED: [2025-11-11 mar. 21:25]
<2025-11-12 mer. 14:04> Getting there :
Enrollement Desk create sutdent/news/ students/  should be removed to point direclty to the Adminj jump
 #+BEGIN_SRC plantuml :results file link :file Docs/view_routing_map3.png
@startuml
skinparam actorStyle awesome
left to right direction

actor Visitor
actor Student
actor "Faculty & Staff" as Staff

rectangle "Public Entry" {
  [Landing\n(/)] as Landing
}

rectangle "Unified Portal" {
  [Portal Login\n(/auth/login/)] as Login
  [Role Router\n(/portal/)] as Router
  [Logout\n(/auth/logout/)] as Logout
}

rectangle "Student Surface" {
  [Student Dashboard\n(/student/dashboard/)] as StuDash
  [Course Dashboard\n(/courses/)] as Courses
}

rectangle "Staff Dashboards\n(/staff/<role>/)" as StaffDashboards {
  [VPAA Approval Hub\nrole=vpaa] as StaffVPAA
  [Dean Oversight\nrole=dean] as StaffDean
  [Chair Curriculum Center\nrole=chair] as StaffChair
  [Instruction Hub\nrole=faculty] as StaffFaculty
  [Registrar Officer Console\nrole=registrar_officer] as StaffRegistrarOfficer
  [Registrar Lifecycle Ops\nrole=registrar] as StaffRegistrar
  [Enrollment Officer Desk\nrole=enrollment_officer] as StaffEnrollOfficer
  [Enrollment Desk\nrole=enrollment] as StaffEnroll
  [Finance Officer Control\nrole=finance_officer] as StaffFinanceOfficer
  [Finance & Holds\nrole=finance] as StaffFinance
  [Cashier Station\nrole=cashier] as StaffCashier
  [Scholarship Office\nrole=scholarship] as StaffScholarship
  [IT Support\nrole=it] as StaffIT
  [Staff Workspace\nrole=staff] as StaffGeneral
}

rectangle "Enrollment Tools" {
  [Student Snapshot\n(/students/)] as StuList
  [Student Detail\n(/students/<pk>)] as StuDetail
  [Admin Jump\n(/students/admin-edit/)] as AdminJump
  [Create Student CTA\n(/students/new/)] as StuNew
  [Delete Student\n(/students/<pk>/delete/)] as StuDelete
}

rectangle "Registrar Tools" {
  [Course Windows\n(/registrar/course-windows/)] as RegWin
}

rectangle "Admin" {
  [Django Admin\n(/admin/)] as AdminPage
}

Visitor --> Landing
Landing --> Login
Student --> Login
Staff --> Login

Login --> Router
Router --> StuDash
Router --> StaffGeneral
Router --> AdminPage

StuDash --> Courses

StaffGeneral --> StaffFaculty
StaffGeneral --> StaffRegistrar
StaffGeneral --> StaffFinance

StaffVPAA --> StaffDean
StaffDean --> StaffChair
StaffChair --> StaffFaculty

StaffRegistrarOfficer --> StaffRegistrar
StaffEnrollOfficer --> StaffEnroll
StaffFinanceOfficer --> StaffFinance

StaffRegistrar --> RegWin
StaffRegistrarOfficer --> RegWin
StaffEnroll --> StuList
StaffEnroll --> AdminJump
StaffEnroll --> StuNew
StuList --> StuDetail
StuDetail --> StuDelete

StaffDashboards --> AdminPage : Admin CTA (shared)
StaffFinance --> AdminPage : Audit invoices
StaffFinanceOfficer --> AdminPage
AdminPage --> StaffGeneral : “Back to dashboard” link

Student --> Logout
Staff --> Logout

@enduml
#+END_SRC

#+RESULTS:
[[file:Docs/view_routing_map3.png]]
