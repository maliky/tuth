
* <2026-01-18 dim. 22:52> Registration/reservation workflow
  - Think of where to block the multiple registrations of a student for a curriculum course in the same semester. I want to give him 2 chances separated by 48h. this Sould be computed on the fly, looking at the registration table history. no stored field, jsut computed values. check that the current workflow is compliant with this propoistion.
  - Not sure about the naming of reservation.  undo the previous changes.  lets have registration every where (code, comments included), the distinction will be the status.
** Refactoring
- Weekday parsing logic duplicated acrosse 2 import commands
- Time parsing logic duplicated with identical formats
- location splitting logic duplicated (include fall back in shared version)
- college/departments code normaliztion repeated in admin widgets and ensures
- academic-year normalization redundant.  Some function are alias or wrapper on others.
- Extract a small pure helper like _coerce_int(value: str | int) -> int | None that returns None on failure, and have: parse_int call it directly, and to_int wrap it and apply default when None.
     

* DONE <2026-01-18 dim. 21:35> Reservation logic student
  CLOSED: [2026-01-18 dim. 22:52]

  - A student registration going back from pending payment to not register should not be available for reregistration straigh away but one should wait 48 h before been able to redo the reservation.  We could also count tath after 2 registration attempt the cours not be available for this semester again.
  - add a message on the student dahsboard to make this rule clear.
** Documenattion for internal function / method
- Always add a documentation string, even a single line to internal function/method.
- Do that in student_dashboard.py
** student_dashboard improvements
- Save reservation does not send the line to Current and past courses as register with pending payment status. check that save reservation does create a registration.  Harmonize terminology. Let's call it Reservation, so it should be Reservation Cart and not Registration Cart.
- Cancel button in Current and past course does not remove the registration/reservation line. it should.
- A course in registered (even with status pending payement) should not be listed in course registration.  it should appear but grayed out.
- The course and Grades list group the courses per semester for older to newer. they should be listed for newer to older (semester key). keep the intra group ordering alphabeticaly for the courses.
- Once Course have pending payment the total amount to be paid should be displayed in the outstanding balance.   

* DONE <2026-01-18 dim. 19:32> Registration fro student/dashboard
  CLOSED: [2026-01-18 dim. 21:35]
** high priority
- [X] add a username column to admin/people/...
- in student/dashboard
  - current and past courses : add a button to cancel the reservation
  - course & grades: order the semester from recent to old and inside each group the by alphabetical order of course
  - Credits completed: remove the card in particular  total ie ''    required_credits = (
        student.curriculum.programs.aggregate(
            total=Sum("credit_hours__code"),
        ).get("total")
        or 0
    )
    remaining_credits = max(required_credits - completed_credits, 0)
'' cannot be correct because students has options and elective courses.
  - in the Registration cart replace the credits remaining by a sum of selected credits.
  - link the save reservation button to the creation of 'reservation' ie registration that will removed after 48h if the state stays pending payment (not that we will not implement this yet, the state will be changed by finance).
  - remaining amount should be stored.    

** Low priority  
- There is too much boiler plate code, make checks and rechecks for None or existence when the code workflow already provide the check up the line and there is no chance of calling subquent function with non existent objects.
- Change all my typing hint of format Class | None => Optional[Class]  it is more readable for me.
- As you work through the code try to identify  and update place where this boiler plate code can be reduced.
  
** DONE 45 mintues
• Updated the student dashboard flow to create/cancel pending reservations, post selected
  section IDs, and show credits selected; grade groups now sort most-recent-first with per-
  semester course ordering, and outstanding balance reads directly from remaining amount_due
  (app/website/views/student_dashboard.py, app/website/templates/website/
  student_dashboard.html, app/website/static/js/student_dashboard.js). Payments now adjust
  invoice remaining balance when a payment is cleared (app/finance/models/payment.py), and
  related helpers got Optional typing cleanup (app/finance/admin/core.py, app/registry/admin/
  core.py, app/website/views/student_helpers.py, app/timetable/models/semester.py, app/
  timetable/utils.py).

* DONE <2026-01-18 dim. 12:31> Registration
  CLOSED: [2026-01-18 dim. 19:32]
- In 'admin/registry/registrationstatus/'
  - for registration column add link to the registrations counted
- Propose plan to update invoice when a payment is made. the remaining amount should be updated.

* DONE <2026-01-18 dim. 12:24> Payments
  CLOSED: [2026-01-18 dim. 19:32]
- in app/finance/admin/core.py,
  - _get_opne_registration_semester seems to be a duplicate. Check that we do not define a similar function else where in the code base and if we do, import the function after factoring the duplicated definitions to an appropriate place such as app/timetable/utils.py. The refactor should be rewritten in a more functional style.
  
- in 'admin/finance/invoice/add/'
  - [ ] set "recorded by" to current logged in staff/user (done but need to handle superuser, see in code comments)
  - [ ] "Recorded by" field should be searchable either by name or by student id. as for the student field.
  - [X] Display searchable short name of scoped curriculum course
  - [X] set defaults for semester to semester opened for registration
  - [X] scope curriculum course to those of the opened_semester

** General observations
  - [ ] the default display for an AbstractPerson should be its longname and ID in parenthesis.  No need to display the user name except on the admin page of the specific AbstractPerson
  - [ ] Any field where we select an AbstractPerson should be searchable either by full name or id.
* DONE <2026-01-16 ven. 16:40> merging
  CLOSED: [2026-01-18 dim. 12:30]
I am trying to merge this

Course
Department
Curriculum
Sections
Faculty
	HIST-475 - Running While Others Walk: African Per…	(COED) HIST	




1	Pius WLALEE
	HIST-475	(COED) HIST	




10	Default User
::  but it is skipped because of incompatible selections
I course have the same department and curriculum. I am expecting a new form asking me to slect fiedl per field the target value. I expect sections to be merged and the faculties to be combined, ie, nothing to do there, will be handled when sections point to the same curriculumcourse.

* DONE <2026-01-16 Fri 16:28> UX 8 so
  CLOSED: [2026-01-18 dim. 12:31]
    P0 – Blockers / correctness

  - Admin / Registry / RegistrationStatus: add a column with registration count.

  P1 – High‑impact workflows

  - Admin / Academics / CurriculumCourse: add merge action with default target, validation messaging, and graceful
    failure for incompatible selections.
  - Decide merge rules for CurriculumCourse:
      - Section conflicts merge
      - Invoice conflicts keep target
  - Admin / People / Faculty: show bio field in change view.



* <2026-01-16 Fri 14:57>  UX 8
  Priority Order

  - P0 — Data correctness/blockers: RegistrationStatus population; curriculum filter error in Course admin; Grade
    search 500; Section admin disallowed filter lookups (400).
  - P1 — High‑impact workflows: CurriculumCourse merge action w/ validation + default; Faculty merge action; Student
    change view grades.
  - P2 — Admin usability: curriculum/faculty links + counts; GPA/credits columns; performance fixes; timetable
    listings.
  - P3 — Cleanup: remove legacy import references; minor UI polish.

  Tracker

  - Done — Student dashboard: courses/grades by semester with semester GPA; total validated credits shown.
  - Done — Admin Department list: curricula column added (via course → curricula links).
  - Done — Admin Timetable Section: curriculum filter added; department/college filters removed (confirm this matches
    intent).
  - Todo — Admin Registry RegistrationStatus: add registration count column.
  - Todo — Admin People Student change view: show grades list.
  - Todo — Admin Academics Course: fix “By Curriculum” autocomplete error.
  - Todo — Admin Academics CurriculumCourse: add merge action with defaults + validation message on selection page.
  - Blocked — CurriculumCourse merge: needs conflict policy for Sections + Invoice handling + default target choice.
  - Todo — Admin People Faculty: merge action with field‑level override selection; align with AbstractUser merge
    mechanism.
  - Todo — Admin Registry Grade: student name search 500; slow change view.
  - Todo — Admin Timetable Section: disallowed lookup errors for semester/year and curriculum/college filters.
  - Todo — Admin Academics Curriculum change view: show student count per course + link.
  - Todo — Admin Academics CurriculumCourse: filters not working; add tests for custom filters.
  - Todo — Admin People Student list: GPA sort logic; add validated credits + latest enrolled semester; fix
    class‑level labels.
  - Todo — Admin People Faculty: sections list duplicates; rename to “Sections/Courses taught”.
  - Todo — Timetable UX: faculty link in sections list; SecSession links + faculty filter; AcademicYear/Semester
    counts; Schedule/Semester change view info fields.
  - Todo — Registry: ensure RegistrationStatus table is populated on init.
  - Todo — Cleanup: remove references to import_directory_people.py.
  - Todo — Faculty: bio field not showing.

  Decisions Needed (to unblock CurriculumCourse merge)

  1. Section conflicts: merge & de‑dupe; renumber; or skip conflicts.
  2. Invoice conflicts: keep target + report; move source to target; or keep both by not deleting source.
  3. Default target: first selected item or explicit choice in confirmation form.
  
* <2026-01-05 lun. 07:33>  Runing a sql cmd
docker-compose -f docker-compose-preprod.yml exec -T db psql -U tusis_preprod -d tusis_preprod -c "SELECT version();"


* <2026-01-05 lun. 07:19>
- Make test for NameParts if I have empty parts.

* <2026-01-06 mar. 18:00> Notes on seed_full_student_grades.py
- Target reader: intermediate Python/pandas engineer; comfortable with typing, argparse, set ops.
- Typical US comp for that level: ~$110k–$150k base; higher with equity/bonus in SF/NYC/Seattle, closer to ~$100k–$120k in lower-cost areas.
- To increase abstraction difficulty: introduce pipeline composition/IoC for loaders+savers, plugin registry for scoring strategies, structured logging/metrics with failure policies, config-driven IO/validation, and immutable data models with strict mypy coverage.
