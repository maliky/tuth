
* <2026-02-18 mer.>  students with several enrollements

** <2026-02-18 mer. 11:58> plan
  Anchors from current codebase:

  - app/registry/choices.py grade codes are A, AB, B, C, D, DR, F, I, IP, NG, W.  - E is not currently a defined grade code.
 
 
  - Current merge behavior in app/ac
 
    retain source because overlapping grade rows are skipped then PROTECT
    prevents section delete.

  So yes, your rule can be the conflict resolver:

  - Rank: A > B > C > D > F > all other non-letter statuses.
  - For overlap on same student + same course + same semester:
      - higher rank wins,
      - if equal rank and equal code, keep earliest section (or target-by-
        default),
      - if equal rank but different non-letter statuses, keep earliest section.
  - Then collapse to one surviving grade row so section merge can complete.

  For your PHIL101 example:

  - Student 4420 with two F values: deterministic merge to one row is
    straightforward (same rank/code -> earliest section wins).
  - If one later attempt is A and earlier was F, A wins by precedence.



** <2026-02-18 Wed 11:54> About Grade policy
Credits versus Class Standing
Grading System

Each course taken at William V. S. Tubman University is assigned a credit value in semester hours. A semester hour/unit is defined as fifty (50) contact minutes and where applicable three hours per week of practical or laboratory work throughout the semester. A semester is fifteen (15) weeks. The William V.S. Tubman University Grading System uses the letter grade with equivalent credit points for the evaluation of academic performance. The numerical value of each letter grade with credit points is as follows:

Numerical Value, Letter Grade and Index Number
NUMERICAL
VALUE
LETTER GRADE
INDEX NUMBER
90-100
A-Excellent
4.0
80-89
B-Good
3.0
70-79
C-Average
2.0
60-69
D-Poor
1.0
Below 60
F-Failure
0.0

Grades below C and Academic Implications

Grades of D, I, F, AB, DR, or NG are considered deficiencies in a student’s academic status at the university.

    • A grade of D is unsatisfactory. A student must re-enroll in the course within the next two consecutive semesters and earn a grade of C or better to earn the course credits in the areas listed below. A course in which a grade of D was earned can only be repeated ONCE. The grade earned when the course is repeated replaces the first grade earned, even if the second grade is lower.

        ◦ A student cannot graduate with a D in:
            ▪ English 101 or 102
            ▪ A major or minor area of concentration
            ▪ More than two (2) courses

    • A grade of I indicates a student may have completed a substantial part of the course with satisfactory performance but has not been able to complete the course. The student has provided documented evidence for the inability to complete the course. The grade of I may be converted to a letter grade when the course requirements are satisfactorily met, within the next two consecutive semesters.

    • A grade of F represents failure. A student who has failed a course cannot  have that grade changed by retaking the final examination or completing additional assignments. A student who fails a course must re-enroll in the course and earn a grade of C or better to earn the course credits.

    • A grade of AB indicates that a student did not take the final examination. Documented evidence has to be provided for the inability to take the final examination. A grade of AB may be converted to a letter grade when the course requirements are satisfactorily met, within the next two consecutive semesters.

    • A grade of DR indicates that a student has exceeded the three (3) excused absences allowed. To earn course credits, the student must re-enroll in the course and obtain a grade of C or better.

    • A grade of NG indicates a student registered for the course but failed to officially withdraw or attend classes. To earn course credits, the student must re-enroll in the course and obtain a grade of C or better.
    • A grade of W indicates a student filed the official withdrawal forms in the Office of the Registrar after consultation with the instructor and received approval from the department chair and/or dean of the college. The day the form is filed is the official date for the withdrawal.   

** <2026-02-18 Wed 11:54> Initial

  At this moment you are my duck, a smart programming assistant /collegue whith whome I can exahcnage idea to relect on the process and clarify them.  analyse the text below and engage a thoughfull conversation. do not edit the code, but do make verification to anchor the examples.

› Review the above plan, update it if necessary. the goal is to handle the
  merging of courses such as 321 (PHIL101) and 168 (PHIL101) which is
  currently stoped because of overlapping grades. in this example student 4420
  has 2 F grades for the PHIL101 courses, in 2 distincs curriculums and
  therefore the grades are linked to 2 curricrs, 6631 and 2094.  After the
  course merge I expect student 4420 to have only one PHIL101 course, the
  earliest one by default. the student 4420 is a special student because he
  enrolled for a second bachelor after having completed a frist one. since PHIL101 is in both of his curriculum there's a conflict that needs disambiguation.

if he obtained a passing grade for his first sectionm then no need to tkae the cours again.  the grade/section of the frist program he took should appear and be incorporated in his 2nd program.

second case.  he could have failed the first program all togehter and enrolle in a different program without having validated the first section. i am toalink of PHIL101.  in that case what ?
I suggest that we apply a similar general pattern, as if the section was copied to the new program, but this cannot be the case exactly because the schedule , effective counts depend an the true relation and the student in this case has to take the classe again.  Therefore he should be added to the section for th enew program.

The solution could come form the registration.
If the sutdent succeeded the first phil courses, he is not required to register again for this course and the previsou section grade is reported to his new progra, and show as is on his grade roster for eg.  this mean alos that either the authorised person can remove some of the prerequisites needed to be fullfiled to display the courses to be registered, or taht the prerequisites can extend and take in account to courses outside of the curent cruricluum.

In the csse where the student did not finalize his first course, then he has to register for the second course.  it is the grade of this course that will now take precedence on the previous coures grade.   but proble. just overideing the previous course grade on officail transcript will brake the rational of for eg, not meeting prerequisites to do such or such course in the first program.  Therefore at the inimun we shold log in the infor field of the grades the changes made.  or we could disociate the grade sheets with a timestmp mechnisme, to the current representation of tin the tusis system.




* <2026-02-17 mar.>
Look at section 66132 and 31954.  the associated curriculum courses should be merged.
course 2696 and 130 seen to block the merging of curriclum or grades.  I am not clear why those 2 course cannot merge. and if it's not the courses the blockage where is it ?
A student cannot be doing 2 section for the same course during the same semester. if merging the course provoc a section clash, then merge the section for that student.  What I want here is that the grade for student 5265 in his  2 BIO101 course be merged into one.

This is the message from the action log '''Retained 33 conflicting section(s) because grades still protect them.
Could not delete 5 source course(s) because grades protect related sections.''' I thnik we have sql scritp giving more details.

Also we have admin action or sql script to merge courses by short code.


I want ot make sure that the admin/academic/course is well sorted by course.shortcode


* <2026-02-17 mar.>  Working on interface again
I need to harmonize the staff interface with what they know.

* <2026-02-16 lun.>  Cleaning DB
I would like an admin action on curricourse which does the following : look at the department of the curricourse curriculum and reset the curricourse to the depatment.college.get_default_curriculum (not exact just for the idea).  This change of curriculum should use the function we defined and handles nicely the merging of duplicates courses.

** <2026-02-16 lun. 10:45> Renaming

  - Course -> Crs
  - Courses -> Crss
  - Curricula -> Curra
  - Curriculum -> Curri
  - Default -> Dft
  - Department -> Dpt
  - Document -> Doc
  - Enrollment -> Enroll
  - Evaluate -> Eval
  - Filter -> Flt
  - Group -> Gp
  - Historical -> Histo
  - Identity -> Idty
  - Inline -> IL
  - Message -> Msg
  - Registrar -> Reg
  - Registration -> Regio
  - Requirement -> Req
  - Section -> Sec
  - Semester -> Sem
  - Student -> Std
  - Widget -> Wgt
  - Window -> Win

* <2026-02-15 dim.>  Understanding duplications problems

** <2026-02-15 dim. 22:38>
cat scripts/curriculum_merge_with_audit.sql | docker-compose -f docker-compose-preprod.yml exec -T db psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -v source_id=206 -v target_id=31 -v apply=0 -v run_label='curri_206_to_31_dry'


  -- latest runs
  SELECT run_id, run_label, apply_mode, status, started_at, finished_at
  FROM scripts_curriculum_merge_run
  ORDER BY run_id DESC
  LIMIT 10;

  -- full logs for one run (replace 8)
  SELECT log_id, stage, entity, source_id, target_id, section_id, reason_code, detail
  FROM scripts_curriculum_merge_log
  WHERE run_id = 8
  ORDER BY log_id;

  -- blocker/conflict view for one run
  SELECT *
  FROM scripts_curriculum_merge_log
  WHERE run_id = 8
    AND (reason_code LIKE '%BLOCK%' OR reason_code LIKE '%CONFLICT%' OR reason_code LIKE '%PROTECTED%' OR reason_code LIKE '%SKIPPED%')
  ORDER BY log_id;


** <2026-02-15 dim. 20:01> refactoring the academics/admin/merges
That file is way to big and need to be split.  I see a split per action functionality + helpers + shared helpers


** General info
 This student Cletus Gray (cletus.gray) 261218 has 2 grades for pe101 the proble is that 19-20 is failed when 18-19 is good. Why is that ?

** Mergin
   What is stoping the merging of grade 338995 and 141394 ?  I would like to merge curricourse 5974 and 1437 or course 2767 and 166 or curriculum 31 and 206.
 reall the perceived objective, then query the db to retreive info on the element mentionned, then propose a wayt to fullfill the objective in a elegant manner.


* <2026-02-12 jeu.> Solving renaming pb
* <2026-02-11 mer.>  big merge
merging curriculums/ keeping grades
I want to switch to protect now. We will make that change on the dev branch. But I just merge the dev and tx branche probably introducing some regressions.  So the first task is review the merge and postnetial regressions.
two, update the code to protect the grades at teh DB level
three develop admin tools to reassign grades in bluck.  Thos tools should be admin action calling sql scripts stored in scripts folder.


** <2026-02-11 mer. 17:10>  merging grades/sections
I want to caputre this rule for merging section with grades between two distinc
  curriculum.  if the grades attached to the 2 sections attached to the same
  course through the curriculum course have the same value, they must be merged.
  keeping the lowest section number, and putting default in all other fields in
  case of conflict on a field (2 differing non default values set for a field) the
  set value always wins on the default.  all non default values must be logged in
  the info field of the merged section.

*** discussion 2
# > keep lowest existing section number among merge candidates.
# > “Merge section when grades are same” only for overlapping students with 2 section of the same course





*** discussion
  How far we are from your expected merge rule (2026-02-11 17:10):

  - Already aligned:
      - Reassignment behavior exists: sessions, grades, registrations are moved
        from source section to target section.
      - Protected retention exists: if grades remain on source, source section is
        retained (delete blocked by PROTECT).
  - Not aligned yet:
      - No grade-value gate before section merge. Current logic does not require
        “same grade value” to merge.
      - No explicit “keep lowest section number” policy.
      - No field-level conflict reconciliation policy for section fields.
      - No default-vs-non-default resolver for section metadata.
      - No logging of non-default source values into Section.info.

  Inconsistencies/ambiguities in requested behavior:

  1. “If grades … have the same value, they must be merged” is ambiguous:
      - per overlapping student?
      - across all grade rows globally?
      - what if students differ between sections?
# > No I am talking here of a student with 2 grades for the same course, but the course are in different program, so different curriculumcourses attached to the same course. This should not be allowed and merge.  The busines logic rule is that if a student redo a new curriculum where he has to repeat a course he already did, we use the garde of the first course and he does not have to repeat the course in practice	
  2. “Put default in all other fields in case of conflict” conflicts with “set
     value wins on default.”
  3. Scope says “between two distinct curriculum,” but current merge utilities also  merge within same curriculum; confirm whether this new rule should apply only to cross-curriculum merges.
     # > The case should not exists in one curriculum because we have unikness enforcement on course atttached in a curriculu. when we reconcile the uniq course we may end up with several section for that course and a specific student. that is where the problem lies. but in fact a student can have severa section of the same course when the first section was not validated. no grade enableing the section to be validated. in that case he will have to retake the course in another section (otehr semester). but if the section was validated, there is no reason to have additional sections. unless we accept that a student is allowed to redo as manyh times as he want what ever the reason a course.  Do we have uniauenss constraint stopign that currently in the db ?

# >     That been said, to my konwledge this has never been the case we we should not see the case arise in the db. Therefore the rule must be enforce to limite eachstudent to one successfull section per course (what ever the curriculum)

  4. “Keep lowest section number” needs scope:
      - only when two sections are merge candidates?
	# > yes
      - or should it renumber non-conflicting sections too?
	# > no

# > The update rule is simply to set the section number to default (0 or 1)





** <2026-02-11 mer. 11:44>  Plan

*** DONE Merge regression audit (no schema changes yet) <2026-02-11 mer. 12:05>
CLOSED: [2026-02-11 mer. 14:11]

  - P0 App cannot start because of unresolved merge markers in app/people/admin/resources.py; python manage.py check fails with SyntaxError.
  - P1 Merge artifacts in app/people/admin/widgets.py: duplicate UserWidget class and undefined names (parse_name, default_password, username) flagged by flake8.
     This is a high regression risk for people import/admin widget flows.
  - P1 app/shared/fuzzy_matching.py is internally inconsistent: undefined top_n, undefined canonicalize_name, and call-site mismatch (top_name_matches(..., top_n=...) while function defines limit). Matching logic likely regressed for import/dedup flows.


**** Lets fix P1 first.
Take origin/ts as reference, as it was working good on it.  so edit the current code to make it match with branch origin/ts even thos we merged it.
*** Protect grades at DB level

  - Change Grade.section from CASCADE to PROTECT in model code.
  - Update delete paths/admin messaging so users get explicit “protected by grades” errors instead of raw tracebacks.
  - You run migrations manually (makemigrations/migrate) on dev; I’ll stop at code-level migration-ready changes.

**** DONE <2026-02-11 mer. 15:52> precision on mycorrection
CLOSED: [2026-02-11 mer. 16:00]
two importants words in all your edits should be abstraction and simplification.
simplification means that things should not be duplicated. for exemple top_n and limit are redundant in fuzzy_matching.  we need to remove one (the most specific usualy)
simplification can also be obtain through abstraction.
In other words make elements simpler but do  not hesitate to assemble them in more abstract units.

**** DONE <2026-02-11 mer. 14:28>  Comments on edits
CLOSED: [2026-02-11 mer. 16:00]
delete_model seems too  general to be only in timetable. move it to shared
Same thing for deletequery set. discuss why not using it as a shared utils ? is ti because of the super() then pass the object as param.

in fact I see the same (abstractidely ) functions in CurriculumAdmin

under which condition can a protected model like section be deleted ?
is it rather the object to which it is attached that is preotected, eg if a grade existe with a section x then section x cannot be deleted. but if no grade existe for section x then seection x can be deleted.


Other thing. I still have many mypy errors from the merge.  Corrected using origin/tx as references
All done! ✨ 🍰 ✨
1 file reformatted, 303 files left unchanged.
app/people/repositories.py:24: error: "NameParts" object is not iterable  [misc]
app/people/repositories.py:25: error: Cannot determine type of "first"  [has-type]
app/people/repositories.py:25: error: Cannot determine type of "last"  [has-type]
app/people/repositories.py:25: error: Cannot determine type of "middle"  [has-type]
app/people/repositories.py:31: error: Cannot determine type of "first"  [has-type]
app/people/repositories.py:32: error: Cannot determine type of "middle"  [has-type]
app/people/repositories.py:33: error: Cannot determine type of "last"  [has-type]
app/people/repositories.py:34: error: Cannot determine type of "prefix"  [has-type]
app/people/repositories.py:35: error: Cannot determine type of "suffix"  [has-type]
tests/shared/test_fuzzy_matching.py:28: error: Value of type variable "AbstractPersonT" of "top_name_matches" cannot be "str"  [type-var]
tests/shared/test_fuzzy_matching.py:28: error: Unexpected keyword argument "top_n" for "top_name_matches"  [call-arg]
app/shared/fuzzy_matching.py:107: note: "top_name_matches" defined here
tests/people/test_utils.py:6: error: Module "app.people.utils" has no attribute "ensure_unique_usernames"  [attr-defined]
tests/people/test_utils.py:6: error: Module "app.people.utils" has no attribute "x"  [attr-defined]
Found 13 errors in 3 files (checked 342 source files)


**** DONE protects courses grades <2026-02-11 mer. 14:04>
CLOSED: [2026-02-11 mer. 16:01]
Currently grades are = PROTECT,

For your curriculum-merge UX request (manual conflict choices), that design is good and implementable. I would build it as:

  1. Restrict action to exactly 2 curricula selected.
  2. Show an intermediate admin form page after action click.
  3. Form sections:

  - target/source curriculum field choices,
  - auto-move summary for non-conflicting courses,
  - conflict table for overlapping courses with per-course choice.

  4. Per-conflict choices:

  - keep target programmed course,
  - keep source programmed course,
  - try merge sections/grades,
  - skip (retain both, no destructive merge for that course).

  5. Execute inside one transaction, then show a result report with moved/skipped/
     protected counts.




*** Admin actions that call SQL scripts

  - Add admin action(s) on CurriculumCourse (and optionally Section if needed) to
    trigger preview/apply scripts.
  - Action flow:
      - select sources + choose target,
      - preview result first,
      - confirm apply,
      - show result counts in admin messages.
  - Execute SQL via Django DB cursor, reading SQL text from scripts/ (no hardcoded
    SQL blobs in admin code).

    # for this it may be convenient for report, or extract model check to run the action through the ORM.  Still I like the idea of having DB action script centralized in the script folder.  how can I get the best of both world ?  Should I ? Maybe one way is obviously better than the other.

*** Hardening and rollout on dev

  - Add focused tests for protected delete behavior and reassignment action logic.
  - Validate grade import behavior after protection (no accidental duplicates,
    expected ProtectedError paths).

  2. Mapping rule for reassignment: same semester + same section number, or
     explicit target section/course mapping per action?


* <2026-02-10 mar.> Tests local

** <2026-02-10 Tue 16:07>  UX/UI tests failures to check latter BDD
  - 5 failed, 209 passed, 65 xfailed
  - The date/datetime TypeError is not present in this latest run.
* <2026-01-01 jeu. 09:13> names parts
  Could harden split_name with NameParts
Make test for NameParts if I have empty parts.
empty should be allowed and default to "" every where except in last_name.


  Remaining failures are all Selenium/BDD:

  1. Registrar dashboard UI flow failures (tests/bdd/test_registrar_grades_dashboard_bdd.py)

  - Timeout waiting for semester filter element.
  - Timeout waiting for pagination area / go-to field.
  - Missing "Official transcript" link.
  - Pattern: expected UI elements are not present when the assertions run.

  2. Student registration BDD failure (tests/bdd/test_student_registration_bdd.py)

  - Timeout after clicking register: selected section value does not clear.
  - Pattern: submit action likely not completing successfully in UI flow (or UI state not updating).

  Notably:

  - No Django internal server error tracebacks in this latest log; these look like UI-state/selector-flow issues, not    backend crash errors.

  If you want, I can now debug these 2 buckets directly (start with registrar BDD page-state checks, then student
  registration submit flow).



** <2026-02-10 mar. 09:27>  Copy from pre-prod to dev
I want to focus a litte bit more on tests.  and set my local dev plateform to  debug effectively.  I've already copied the preprod db localy with a manual
#+BEGIN_SRC bash
  ssh user@host 'cd Tusis/Tusis_app && docker-compose -f docker-compose-preprod.yml exec
  -T db pg_dump -Fc -Z 9 -U tusis_preprod -d tusis_preprod > /tmp/tusis_preprod.dump'
  rsync user@host:/tmp/tusis_preprod.dump /tmp/
  sudo -u postgres dropdb --if-exists --force tusis
  sudo -u postgres createdb -O tusisuser tusis
  sudo -u postgres pg_restore -v -d tusis --no-owner --no-privileges --role=tusisuser /tmp/tusis_preprod.dump
#+END_SRC
# Quick sanity check OK
#+BEGIN_SRC bash  -i
psql -U "$DJANGO_DB_USER" -h "$DJANGO_DB_HOST" -p "$DJANGO_DB_PORT" -d "$DJANGO_DB_NAME" -c "SELECT count(*) FROM django_migrations;"



* <2025-12-20 Sat 11:15> Importing matcher

• I dug through the codebase:

  - app/shared/fuzzy_matching.py defines name_similarity, but it currently calls    sim_name_token, which is commented out—so calling this function will raise a    NameError. It also relies on Python’s difflib.SequenceMatcher for both
    surname and given-name scoring, with custom weighting/penalties.
  - app/finance/utils.py doesn’t have any name-similarity logic; it only has
    tuition helpers. The only other name-matching utilities live in app/shared/
    importing/username_matching.py (Jaro-Winkler similarity for usernames) and
    app/people/utils.py (name_distance, now order-insensitive and backed by
    rapidfuzz Jaro-Winkler).

  Given that, I prefer the Jaro-Winkler-based approach (as in people/utils.py or  shared/importing/username_matching.py) because:

  - It uses a well-tested, fast C-backed implementation (rapidfuzz) instead of
    Python-level SequenceMatcher.
  - It produces a normalized distance/similarity, is symmetric, and—after
    canonicalization/sorting tokens—handles name reordering more gracefully.
  - It doesn’t depend on a missing helper (sim_name_token), so it won’t throw.

  If you want to improve scalability for ~10k×10k name comparisons (100M pairs),  avoid Python loops and leverage vectorized/compiled distance routines:

  - Use rapidfuzz.distance.JaroWinkler.cdist(left_list, right_list, workers=-1)
    (or process.cdist) to compute a full similarity matrix in C with
    multithreading. You can then threshold or keep top-k per row without holding    the full matrix by streaming/chunking.
  - Pre-canonicalize and lowercase all names once; optionally sort tokens to
    make comparisons order-insensitive as in people/utils.py.
  - If 100M pairs is still too large to hold, block on simple keys (e.g., first
    letter(s) of last name) to reduce candidate sets, then run cdist on each
    block.

  So: fix shared/fuzzy_matching.name_similarity by reinstating or replacing
  sim_name_token (or swap to rapidfuzz), and for large-scale matching, use
  rapidfuzz’s cdist/top-k utilities rather than Python loops.



** ################
  app/people/models/object_manager.py:66: error: Unexpected keyword argument
  "limit" for "top_name_matches"  [call-arg]
  app/shared/fuzzy_matching.py:54: note: "top_name_matches" defined here
  app/people/admin/mixins.py:86: error: Unexpected keyword argument "limit" for
  "top_name_matches"  [call-arg]
  app/shared/fuzzy_matching.py:54: note: "top_name_matches" defined here
  tests/people/test_utils.py:6: error: Module "app.people.utils" has no
  attribute "name_distance"  [attr-defined]
  tests/people/test_utils.py:6: error: Module "app.people.utils" has no
  attribute "names_match"  [attr-defined]
  tests/people/test_utils.py:6: error: Module "app.people.utils" has no
  attribute "name_similarity_matrix"  [attr-defined]
  Found 5 errors in 3 files (checked 256 source files)
  (tusis) mlk@r1d05 ~/Tusis/Tusis_app $
  ::: and update agents.md to help other agent work in our environment


*** proposed work
I can’t apply the fixes directly because the filesystem is read-only, but here
  are the exact changes to clear the mypy errors and add the requested guidance:

  1) Allow limit in top_name_matches

  - File: app/shared/fuzzy_matching.py
  - Update the signature and slicing:

  def top_name_matches(
      base: str,
      candidates: Iterable[_T],
      token_fn: Callable[[_T], str] = identity,
      threshold: float = 0.9,
      top_n: int = 3,
      limit: int | None = None,
  ) -> list[tuple[_T, float]]:
      ...
      scored.sort(key=lambda t: t[1], reverse=True)
      n = top_n if limit is None else limit
      return scored[:n]

  (best_name_match can stay as-is; it already passes top_n=1.)

  2) Restore name-matching helpers in app/people/utils.py
  Add these near the bottom (after canonicalize_name or grouped with the other
  helpers):

  def name_distance(name_a: str, name_b: str, *, prefix_weight: float = 0.1) ->
  float:
      """Return a normalized distance (0=identical, 1=different) between two
  names."""
      canonical_a = canonicalize_name(name_a)
      canonical_b = canonicalize_name(name_b)
      return float(
	  JaroWinkler.normalized_distance(
	      canonical_a, canonical_b, prefix_weight=prefix_weight
	  )
      )


  def names_match(name_a: str, name_b: str, *, threshold: float = 0.2, **kwargs)
  -> bool:
      """Return True when the distance between two names is within a given
  threshold."""
      return name_distance(name_a, name_b, **kwargs) <= threshold


  def name_similarity_matrix(
      left_names: Sequence[str],
      right_names: Sequence[str],
      *,
      max_distance: float | None = None,
      **kwargs,
  ) -> list[dict[str, object]]:
      """Return a list of similarity rows describing pairwise name distances."""
      matrix: list[dict[str, object]] = []
      for left in left_names:
	  for right in right_names:
	      dist = name_distance(left, right, **kwargs)
	      if max_distance is not None and dist > max_distance:
		  continue
	      matrix.append({"left": left, "right": right, "distance": dist})
      return matrix

  3) Update AGENTS.md to help other agents
  Add a short note such as:

  - “You’ll see pyenv: cannot rehash warnings in this environment; they are
    harmless.”
  - “rg may be unavailable; fall back to python/grep.”
  - “Filesystem may be read-only in some sessions; be prepared to provide patch
    instructions instead of applying changes.”

  After applying these, rerun mypy and the errors should disappear. Let me know
  if you want me to apply the patches once write access is available.


›
* <2025-12-18 jeu. 21:49> Terms for students
  Define a rule to set the terms form the studentinfo import
  something like for oldest for  first TermFirstEntered and newest for TermLastEnrolled

* TODO <2025-12-18 Thu 17:06>
2025-12-18 17:02:53,324  INFO      tusis.import :: Starting import for LegacyRegistration
Importing LegacyRegistration: 100%|█████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 8735/8735 [00:01<00:00, 5286.91it/s]
Row 1 failed: duplicate key value violates unique constraint "people_staff_staff_id_key"
DETAIL:  Key (staff_id)=(DFT_STF0000) already exists.
Row 2 failed: An error occurred in the current transaction. You can't execute queries until the end of the 'atomic' block.
Row 3 failed: An error occurred in the current transaction. You can't execute queries until the end of the 'atomic' block.
Row 4 failed: An error occurred in the current transaction. You can't execute queries until the end of the 'atomic' block.
Row 5 failed: An error occurred in the current transaction. You can't execute queries until the end of the 'atomic' block.
CommandError: LegacyRegistration import failed with 8735 errors and 0 invalid rows.
* <2025-12-17 mer. 17:53>  fuzzy matching for curriculums
** You did well but a few observation
- do not abuse of .strip()  I take care of cleaning the data before reaching this deep in the code.
- We should be improve the factoring and code  readability (it's already good but...)
  - in curriculum find_fuzzy_match: the sequence in case of tie, could be factored in a function...
  - The idea is to reuse

- Do not forget to add documentation even if succint
-
** I want to refactor the usage of name_similarity
See in this files
app/people/admin/core.py
31:from app.people.matching import name_similarity
79:            score = name_similarity(base_name, other_name)
app/people/admin/mixins.py
10:from app.people.matching import name_similarity
78:            score = name_similarity(base_name, other_name)
app/people/matching.py
42:def name_similarity(
85:        score = name_similarity(base, name)
app/people/models/object_manager.py
 9:from app.people.matching import name_similarity
68:            score = name_similarity(base_name, candidate_name)

We use a very similar algoritm.
I want to factor it using a something like below:
    # create the base name (tokenA)
    # get candidates
    # initialize storage
    # Loop on the candidates
    #  for each candidate C create the  a comparaison key (tokenB)
    #  score tokenA -- TokenB distance
    #  update storage based of threshold
    # return

    # vars, a function to get candidates or the candidate them selves
    # a function to get tokenA or tokenA itself
    # a function f to get the tokenB : f(C,*args) -> tokenB
    # thresholds to update storage

or even using the already defined function find_similar_names.

Do your analysis on how we can make our code sweeter.

* <2025-12-16 mar. 20:18> codex resume 019b1809-8544-7742-9f25-0c1f412f3aaf
  - when importing UM_student
    - need to do a proximity check on the curriculum with existing
    - need to make sure that all student info is going to the model
      - eg Aaron collins have middle name jumped, no adress inputed
    - in the student admin view filter should only list the curriculum, years and college present in the students

* <2025-12-15 lun. 23:53>  running the tests ?
  working
  need to
    Tusis_app $ sudo -u postgres psql -d postgres
    postgres=# grant create on database postgres to tusisuser;
    sudo -u postgres psql -c "ALTER ROLE tusisuser CREATEDB;"



* TODO <2025-11-11 Tue 17:09> View routing map

 CLOSED: [2025-11-11 mar. 21:25]
<2025-11-12 mer. 14:04> Getting there :
Enrollement Desk create sutdent/news/ students/  should be removed to point direclty to the Adminj jump
 #+BEGIN_SRC plantuml :results file link :file Docs/view_routing_map3.png
@startuml
skinparam actorStyle awesome
left to right direction

actor Visitor
actor Student
actor "Faculty & Staff" as Staff

rectangle "Public Entry" {
  [Landing\n(/)] as Landing
}

rectangle "Unified Portal" {
  [Portal Login\n(/auth/login/)] as Login
  [Role Router\n(/portal/)] as Router
  [Logout\n(/auth/logout/)] as Logout
}

rectangle "Student Surface" {
  [Student Dashboard\n(/student/dashboard/)] as StuDash
  [Course Dashboard\n(/courses/)] as Courses
}

rectangle "Staff Dashboards\n(/staff/<role>/)" as StaffDashboards {
  [VPAA Approval Hub\nrole=vpaa] as StaffVPAA
  [Dean Oversight\nrole=dean] as StaffDean
  [Chair Curriculum Center\nrole=chair] as StaffChair
  [Instruction Hub\nrole=faculty] as StaffFaculty
  [Registrar Officer Console\nrole=registrar_officer] as StaffRegistrarOfficer
  [Registrar Lifecycle Ops\nrole=registrar] as StaffRegistrar
  [Enrollment Officer Desk\nrole=enrollment_officer] as StaffEnrollOfficer
  [Enrollment Desk\nrole=enrollment] as StaffEnroll
  [Finance Officer Control\nrole=finance_officer] as StaffFinanceOfficer
  [Finance & Holds\nrole=finance] as StaffFinance
  [Cashier Station\nrole=cashier] as StaffCashier
  [Scholarship Office\nrole=scholarship] as StaffScholarship
  [IT Support\nrole=it] as StaffIT
  [Staff Workspace\nrole=staff] as StaffGeneral
}

rectangle "Enrollment Tools" {
  [Student Snapshot\n(/students/)] as StuList
  [Student Detail\n(/students/<pk>)] as StuDetail
  [Admin Jump\n(/students/admin-edit/)] as AdminJump
  [Create Student CTA\n(/students/new/)] as StuNew
  [Delete Student\n(/students/<pk>/delete/)] as StuDelete
}

rectangle "Registrar Tools" {
  [Course Windows\n(/registrar/course-windows/)] as RegWin
}

rectangle "Admin" {
  [Django Admin\n(/admin/)] as AdminPage
}

Visitor --> Landing
Landing --> Login
Student --> Login
Staff --> Login

Login --> Router
Router --> StuDash
Router --> StaffGeneral
Router --> AdminPage

StuDash --> Courses

StaffGeneral --> StaffFaculty
StaffGeneral --> StaffRegistrar
StaffGeneral --> StaffFinance

StaffVPAA --> StaffDean
StaffDean --> StaffChair
StaffChair --> StaffFaculty

StaffRegistrarOfficer --> StaffRegistrar
StaffEnrollOfficer --> StaffEnroll
StaffFinanceOfficer --> StaffFinance

StaffRegistrar --> RegWin
StaffRegistrarOfficer --> RegWin
StaffEnroll --> StuList
StaffEnroll --> AdminJump
StaffEnroll --> StuNew
StuList --> StuDetail
StuDetail --> StuDelete

StaffDashboards --> AdminPage : Admin CTA (shared)
StaffFinance --> AdminPage : Audit invoices
StaffFinanceOfficer --> AdminPage
AdminPage --> StaffGeneral : “Back to dashboard” link

Student --> Logout
Staff --> Logout

@enduml
#+END_SRC

Several things.
Context, the machine is good Dell : 24 cpu /heart 16GB ram, graphic card with 8GB.  SSD disk.
I am running mint.
The main test I want to speed up are the UI/UX? tests using selenium ( grid option ?) the UX in particular tests should be Behavioral driven and I already started implementing some.
I need help to finalise the setup.
Secondly more practlicaly I just need a redicrect of the test output to a file for inspection.



* <2026-02-09 Mon> Tests

  - 1e355b1 (finance core SSI refactor)
  - 1164560 (student dashboard fee-stack wiring)
  - 72211f6 (finance officer/admin workflow update)

  ## Key gap pattern from recent commits

  The biggest finance changes landed with little/no direct test additions (especially 1164560 and 72211f6).
  Current tests cover some SSI behavior (tests/finance/test_fee_assignment.py, test_invoice_snapshot.py,
  test_payment.py, and one website cancellation suite), but there are major holes in DB invariants, finance officer
  business rules, and UX flows.

  ———

  ## Loopholes by test category (with finitude level)

  ### 1) DB tests — High finitude (deterministic, strict assertions)

  Missing coverage around:

  - app/finance/models/invoice.py parent/child synchronization rules
  - StudentSemesterInvoice uniqueness and auto-linking from CourseInvoice
  - M2M fee-stack signal recomputation (refresh_parent_invoice_after_fee_stack_change)
:Comment:
M2M fee-stack signal recomputation in your code means:
  - StudentSemesterInvoice has a many-to-many fee_stacks relation in app/finance/models/invoice.py.
  - A Django m2m_changed receiver (refresh_parent_invoice_after_fee_stack_change) runs on post_add, post_remove, post_clear.
  - That receiver calls refresh_totals_from_sources() on the parent invoice.

  What refresh_totals_from_sources() recomputes in app/finance/models/invoice.py:

  - Parent initial_amount_due = sum(course invoices) + sum(attached semester fee stacks).
  - required_deposit_amount, balance, status, and fee_payer resolution (student/gov/mixed).
  - Child course invoice balances/statuses are re-distributed from the parent total.
  - Registration status is updated from child invoice status.

  So the signal is there to keep totals consistent when someone changes fee-stack links from anywhere (admin, dashboard, scripts).

  Pragmatic alternative (signal-light):
  1. Move recomputation to an explicit service like recompute_student_semester_invoice(invoice_id, reason).
  2. Call it directly from write paths (attach_semester_fee_stacks, fee-stack line mutations, payment writes).
  3. Keep a minimal signal only as a safety net (or remove signals after coverage is strong).
  4. Batch operations should recompute once at the end, not per link mutation.

  If you want, I can draft that refactor in this repo with a backward-compatible transition (service first, then thin signals, then optional signal removal).
:END:

  - Payment.save() / Payment.delete() recalc behavior in app/finance/models/payment.py
  - payer-resolution edge cases (student/gov/mixed)

  ### 2) Business logic tests — Medium finitude (scenario matrix)

  Missing coverage around:

  - app/finance/utils.py:create_pending_payments (skip existing, skip closed, payer defaults)
  - app/website/views/finance_officer_helpers.py grouping/filter logic (group_invoices, group_payments,
    invoice_queryset, payment_queryset)
  - app/website/views/finance_officer.py POST handlers (invalid amounts, invalid payer, no selections, permission
    gating)
  - app/website/views/student_dashboard.py optional fee-stack registration path (new in 1164560) is effectively
    untested

  ### 3) UX tests — Lower finitude (critical path, tolerant assertions)

  Missing coverage around:

  - Finance Officer Console end-to-end flow (finance_officer_invoices.html) for:
      - filtering by student/semester/status
      - creating pending payments
      - editing payment fields and validation feedback
  - Student dashboard optional fee-stack selection UX
  - Prerequisite graph TypeScript UI updates (many commits, almost no UI assertions beyond backend export test)

  ## Action plan (practical sequence)

  ### Phase 1 (now): stabilize finance core (DB + business logic)

  1. Add tests/finance/test_student_semester_invoice.py for SSI invariants and recalc triggers.
  2. Add tests/finance/test_create_pending_payments.py for utility scenarios.
  3. Add tests/website/test_finance_officer_views.py (Django client) for permission + POST validation paths.

  ### Phase 2: close regressions from recent commits

  4. Extend tests/website/test_student_dashboard_cancellation.py with optional fee-stack register scenarios (from
     1164560).
  5. Add focused tests for finance helper grouping/filtering (tests/website/test_finance_officer_helpers.py).

  ### Phase 3: UX confidence

  6. Add BDD/Selenium scenarios for finance officer workflows (create/update payment flows + filter persistence).
  7. Add one smoke UI test for prereq graph layout switcher behavior (not full rendering fidelity, just interaction/
     assertable DOM state).

     # > The problem I have here is to set up the infrastructure to run selenium test effectively ( in parallele on a grid for eg.)


* <2026-02-09 lun.>  UX

** <2026-02-09 lun. 20:32> detail plan
*** Add explicit requirement models (keep data structured, no encoded text field).

  - Extend app/academics/models/curriculum_course.py with min_validated_credits (default 0).
  - Add CurriculumCourseRequirementGroup model:
      - FK to target CurriculumCourse
      - kind choices: prereq_all, prereq_any, coreq_all
      - ordering/display fields
  - Add CurriculumCourseRequirementMember model:
      - FK to group
      - FK to required Course
      - constraints: unique (group, required_course), no self-reference.

*** Build clean admin UX for data entry.

  - Register requirement-group admin with inline members.
  - Add filters/search by curriculum, target course, kind.
  - Add quick link/count from curriculum-course admin so staff can navigate requirements without coding
    syntax.

*** Implement requirement resolver in one shared helper.

  - Create a functional helper in academics/website layer (no service class required).
  - Inputs: student, target curriculum course, currently selected course IDs in this registration action.
  - Outputs: pass/fail + machine-readable reasons:
      - missing credits,
      - missing prereq_all,
      - unsatisfied prereq_any,
      - incomplete coreq_all.

*** Enforce strict coreq_all rule in registration flow.

  - In student_dashboard registration POST path, require all coreq members to be selected in the same action.
  - Do not treat previously passed courses as satisfying coreq_all (per your confirmation).
  - Return clear user-facing messages per blocked course.

*** Update availability display logic.

  - In dashboard course eligibility/reason logic, surface:
      - Requires at least X validated credits
      - Must be selected together with ... for coreq failures
      - prerequisite failures by group type.

*** Update graph export/rendering for corequisites.
for this make a new branch to test th feature.
both alternate and corequesistes should be subgraph cluster in dot
for alternate arrow go directly to the nodes in teh cluster
for coreq the arrows to the cluster have lhead=<clustername> and those from ltail=<clustername>
no need to add links between the nodes in teh cluster.  my only concern is how the cluster will position itslef amounb the other node. I experience that it had problem to keep the same rank as the other node of the same level. This is why I wnat this in a separate branch.
for the elk, the difference to the current implemntation of alterante is mainly the heade and tail which should also not enter  the cluster for corequeiste and enter it for alternate.  then the hhighlight should be done around the corequsite as a whole and not just it's elements.

  7. Transition strategy (manual migration by you).

  - Manual DB migration creation/application for new field/models.
  - Backfill script/command:
      - convert existing Prerequisite rows into prereq_all groups.
  - Keep old prerequisite read-path temporarily until backfill is validated, then remove legacy path.

  8. Tests to add (recommended).

  - Model validation:
      - reject self member,
      - reject duplicate member in same group.
  - Rule evaluation:
      - blocks when min_validated_credits not met,
      - prereq_any passes with one member, fails with none,
      - coreq_all fails on partial selection, passes on full same-action selection.
  - View/integration:
      - registration POST returns clear error when coreq set incomplete.
  - Graph export:
      - DOT output contains dir=none coreq ties,
      - DOT output contains rank=same for each coreq group.


** <2026-02-09 lun. 20:06> back to sofisticated prerequisites
in my business logic
- some course are lock until a certain number of credit hours has been validated
- other courses have to be taken together. .
  my model does not capture well this rules.
  I know I coud simply put some rules in code for this but I am looking fo a clean ux for end user to enter those requirements.
  mabye instead use the group of the course and if the group is a char/negative then the course are not alternative but need to be taken together.  this would show as a node containting them in elk and the containeing node would have dashed border for alternate mbember.  this is a suggestion. other are welcome.
  for the elvel requirement, I see it as a new field attached to a course, specifying a number of credit to be obtained to do the course.
  for practical resason all could be centralised in the gorup number converted to a textfield, zith this codeing positive capital letter coprerequisites, lowercqse letter alternate number positive, the number of credit that should have been validated.   could be done with different fields too. what do you propose ?


** <2026-02-09 lun. 13:26> twicks
department names or curriculum/prgram names starting with

** Curriculum UX <2026-02-09 lun. 13:25>
  1. Update ELK graph interaction to support bidirectional highlighting.

  - In app/academics/static/academics/js/prereq_graph_viewer_elk_compound.ts, extend graph indexing from only
    incomingByTarget to both incomingByTarget and outgoingBySource.
  - Keep current prerequisite highlight (ancestors) in blue.
  - Add dependent-course highlight (descendants: courses that require the selected course) in green.

  2. Add separate CSS classes for descendant nodes/edges.

  - In app/academics/static/academics/js/prereq_graph_viewer_elk_compound.ts, add styles like node.is-
    descendant and edge.is-descendant with green palette, while preserving is-ancestor in blue.
  - Ensure selected node style remains distinct and not overridden.

  3. Refactor highlight lifecycle to clear/apply both directions safely.

  - Replace highlightAncestors with a combined handler (e.g., highlightRelations) that:
      - clears prior highlight state,
      - applies blue to ancestors,
      - applies green to descendants,
      - keeps selected node emphasized.
  - Keep parent group nodes excluded from selection behavior as today.

  4. Fix curriculum-course grouping behavior when level becomes “Other (99)”.

  - In app/academics/models/curriculum_course.py, adjust level normalization so changing level_number to 99
    explicitly resets grouping fields (year_number, semester_number) to the “other/undefined” bucket values.
  - This prevents stale old year/semester grouping after a level change.

  5. Make admin group label explicit for the “other” bucket.

  - In app/academics/templates/admin/academics/curriculumcourse/tabular_inline.html, show a clear label like
    “Other grouping” when row group corresponds to level 99/undefined bucket.
  - Keep existing grouping/toggle UX unchanged.

  6. Add a one-time data repair for existing inconsistent rows.

  - Prepare a management command or migration-safe data script to realign existing CurriculumCourse rows where
    level_number=99 but year_number/semester_number still point to prior groups.
  - Run on preprod after deploy so old bad rows move into the new “other grouping” immediately.



* <2026-02-08 dim.>  backend invoice/fee/payment

** <2026-02-08 dim. 13:56>  tHNINKING / PREPARING TEMPLATE
  For the integrated invoice/payment template editor, here is a compact implementation plan:

  1. Add template configuration models

  - DocumentTemplate (code, label, channel, active_version FK, timestamps, history).
  - DocumentTemplateVersion (template FK, content, format, version_no, created_by, timestamps, is_active).
  - Keep explicit template codes like invoice_snapshot, payment_receipt.

  2. Add rendering abstraction

  - One resolver function: render_finance_template(code, context) that fetches active version and renders
    content.
  - Fallback to filesystem template if no DB template exists.

  3. Add finance/registrar editor UI

  - Staff page under finance dashboard (not public admin raw model only).
  - Features: list templates, edit content, duplicate version, activate version, rollback.
  - Access restricted to finance/registrar roles.

  4. Add preview + validation

  - “Preview with sample context” button.
  - Validate required placeholders before activation.
  - Reject activation if required keys are missing.

  5. Integrate current document outputs

  - Wire invoice snapshot and payment receipt generation to resolver.
  - Keep static template fallback during rollout to avoid downtime.

  6. Guardrails + audit

  - History on both template and versions.
  - Activation log entry with actor/date.
  - Optional “lock after activation” policy if needed later.

  If you want, next step I can draft the exact model fields and admin/staff forms (no code edits yet) so you
  can approve the schema before implementation.


** <2026-02-08 dim. 11:47>  Plan to create full blown invoices
  - P1 Pending payments are treated as settled money in balance math. app/finance/models/invoice.py sums all payments in refresh_totals_from_sources, while app/finance/utils.py creates full-
    amount pending payments. Result: creating a pending payment can zero balances and flip invoice/registration statuses too early.

  - P1 Student-facing invoice outputs are inconsistent with the new parent invoice model. app/finance/utils.py build_invoice_snapshot uses only course stacks and hardcodes 40%; it does not
    include StudentSemesterInvoice.fee_stacks totals or required_deposit_percent.

  - P1 Registration cancel flow can drop valid semester-level charges. In app/website/views/student_dashboard.py, parent invoice deletion is based on “no course invoices” and can remove
    invoice context even when semester fee stacks should remain.

  - P2 The “default general fee stack once per semester” use case is still manual. Invoice creation path in app/website/views/student_dashboard.py creates course invoices, but no policy/
    service auto-attaches semester/global stacks.

  - P2 Payer data is modeled but not enforced in workflow. Payment defaults to student in app/finance/models/payment.py, and create_pending_payments in app/finance/utils.py does not derive
    payer from SSI context; finance officer bulk update view does not edit payer either.

  - P2 Permissions are still alias-heavy and fragile. app/shared/auth/perms.py relies on legacy aliases (invoice, coursefee, curriculumcoursefee) that now fan out to multiple models; this is
    easy to misconfigure and hard to audit.

  - P2 Test suite still contains obsolete old-API usage. tests/finance/test_payment.py and tests/selenium/fixtures_finance.py still create Payment(invoice=...) although payments now attach to
    StudentSemesterInvoice.

  - P3 Current worktree has referenced-but-untracked admin filter code (app/finance/admin/filters.py). If not committed, deploy/import can break.

  Updated Fix Plan

  1. Define payment accounting semantics explicitly: only cleared payments affect StudentSemesterInvoice.balance; pending remains informational. OK
  2. Refactor invoice rendering/snapshot to parent-first computation: include semester fee stacks, SSI deposit percent, and keep child course lines as breakdown only. OK
  3. Harden cancellation lifecycle: never delete SSI when semester-level fees or non-reversible payment history exist; recalculate instead. OK
  4. Add a fee-assignment service for invoice generation: auto-attach default semester stacks idempotently, plus optional stacks (dormitory-like) from explicit student choice. OK
  5. Complete payer workflow: keep payer FK model, but propagate payer defaults from SSI into payment creation and expose payer in finance officer update flow.
  6. Clean permissions matrix to explicit current models (no legacy aliases), then validate with load_roles. OK
  7. Refresh tests: remove old Payment.invoice tests, add targeted tests for pending-vs-cleared balance behavior, SSI snapshot totals, cancellation retention rules, and auto-attach     idempotency. OK
  8. Finish rollout hygiene on dev/preprod: ensure migrations applied, ensure seed commands create payer/fee-stack defaults, and commit currently untracked required files. OK



** <2026-02-08 dim. 11:14> How to create the nivoice with the genera stack feee
When generating the invoice, we need to include the course tuitions, depends on course ch and ch cost. this will be taken care by gove for liberian students
we need to add speaclia stack fee dependent on the course type, eg lab fees for some bio or medical courses
we need to add genereal fee like registration fee
on some semesster we need to ad d some other fee for some courses eg pe has a uniforme fee every first semester of the yesar if you take that course.

I did attache the sepcific courses fee package to the special courses (la/enterpreneruship ect...) but for the genera fee this need to be adde when the use creates his invoice has some genera fees depend on the semester and on option of the user.   in fact I am remembering that we neeed to incle dormitory fee for stends who pay dormiroty. wher do they make ta selection ? and where is this store.  I line toward the invoice table for the storage space. for the selection f, should be done at teh student ux level in a different panel from the course registration. or jsut a checkbox for the dormitory. I understna the dormitory are the only addition fee that cannot be deducted from course and semester.


** <2026-02-08 dim. 09:12>

• 1. Finalize the payer contract and naming.

  - Rename Invoice to CourseInvoice.
  - Keep payer fields only on StudentSemesterInvoice: course_tuition_payer (default gov) and
    fee_payer (default student).
  - Add nullable payer on FeeStack and FeeStackLine, with choices including mixed.

  2. Implement one payer-resolution rule for fees.

  - Resolve each fee line payer as: FeeStackLine.payer -> FeeStack.payer ->
    StudentSemesterInvoice.fee_payer.
  - Tuition payer always comes from StudentSemesterInvoice.course_tuition_payer.

  3. Make StudentSemesterInvoice.fee_payer auto-derived when needed.

  - Compute the set of effective fee-line payers for the semester.
  - If set size > 1, force fee_payer = mixed.
  - If set size = 1, set fee_payer to that payer.
  - If no line payer resolves, keep the non-null default behavior.

  4. Keep parent-invoice architecture as source of truth.

  - One StudentSemesterInvoice per (student, semester).
  - CourseInvoice linked under it.
  - Payments linked to parent invoice only.
  - Parent totals/status computed from child course totals + attached semester fees + payments.
  5. Preserve manual governance (no hard guard after payment).

  - Admin can still edit payer/deposit fields after payments.
  - History remains the audit trail.
  - No additional lock mechanism.

  6. Validate with focused scenario checks before rollout.

  - Single-payer stack set -> parent fee_payer single value.
  - Multi-payer stack lines -> parent fee_payer = mixed.
  - Null payer fallback chain works at all three levels.
  - Tuition payer independent from fee payer.


›

** <2026-02-08 dim. 09:07> refinement on payer
default_fee_payer (call it fee_payer ) in StudentSemesterInvoice should be set to mixed if the se of feestackline.payer has more than one payer.
** <2026-02-08 dim. 08:24> defining the payer types
Remove the payer_type from courseInvoice and move it to StuddentSemesterInvoice level. call it course_tuition_payer (non nullable default to gov). add also a default_fee_payer (non nullable) (default to student).
We need to add a payer field to the feeStacks line (nulllable)  and feestack (nullable).
The rational is that sometime we may have the governement taking care of tution, and 3rd partie taking care of one or 2 items of a feestack. option for payer (not payer type) should include mixed.
The fee rule should be as follow, if null check the level above for a payer and use that as default.  the default for top level is always set
** <2026-02-08 dim. 07:56> update on model for better invoice/fee management
We are still  reflecting, no edit.
1. we can drop the guards even after first payment. Control will be manualy by admin
2. do not mention  details for code areas or migration changes.
3. Rename the invoice model to CourseInvoice in DB and UI.
4. your steps 2  to 6 are correct



* <2026-02-02 lun. 07:23> DB sync
1. You run the dump on preprod (redirection happens on preprod)

  ssh user@host 'cd Tusis/Tusis_app && docker-compose -f docker-compose-preprod.yml exec
  -T db pg_dump -Fc -Z 9 -U tusis_preprod -d tusis_preprod > /tmp/tusis_preprod.dump'

  rsync user@host:/tmp/tusis_preprod.dump /tmp/

  sudo -u postgres dropdb --if-exists --force tusis
  sudo -u postgres createdb -O tusisuser tusis

  3. Restore the dump into test_tusis (keep ownership local):

  sudo -u postgres pg_restore -v -d tusis --no-owner --no-privileges --role=tusisuser /tmp/tusis_preprod.dump

#  Quick sanity check:

  psql -d test_tusis -c "SELECT count(*) FROM django_migrations;"



* <2026-02-01 dim. 18:43> Test the prereq graph engine


* <2026-01-30 ven. 23:00> curriculum
add a link from the dean dashboard to the curriculum admin, for him to edit curriculums
he should be able to see all the course, but only edit those courses unders his college.
He should be able to create new courses for his college and change the assignement of the courses to the curriculum of his college.

the  students number  displayed in   admin/academic/curriculum for each row (curriculum) should match the sum of  the student numbers computed in the course inline for that specific curriculum.  Now, in the course inline the student numbers are zero. Check and correct.


* <2026-01-30 ven. 20:59> prerequisites
  If no curriculum set, then it as the default for the course.

* <2026-01-30 ven. 15:01> Invoice template
# The snapshot should occure the first time the user access student_invoice_statement

** I reviewed the PDF at data/bc/93afbf-000f-47eb-bece-93aa309d0dca/
  template_bill.pdf (extracted text with pypdf). Here’s what the template expects
  and how it maps to what we currently have.

*** Fields shown in the template

  - Header:
      - “Registration Bill”
      - University name + address (static text)
      - Date/Time (printed at generation)
  - Student block:
      - Academic Semester (Academic Year + Semester number)
      - ID No
      - Name
      - Department
      - Major  # Curriculum name for us
      - Present Status (freshman/sophomore/junior/senior) # we compute it from the number of credit done, have a method for that already
      - Address
  - Course table (line items):
      - Course Code  # it the course department for us
      - Course No # we have that
	# the 2 above are replaced by our course short code
      - Course Title
      - Credit Hours # from  curriculum course
      - Cost Per Credit (US$) # fixed
      - Cost of Course
      - Total Credit Hours
  - Fee summary:
      - Activities / Library / Maintenance / Registration / Sports / Technology
	(fees list)  # we need to set this precisely. I may have that information in the DB.
      - Fees total
      - Tuition total
      - Total Bill
      - Arrears  # can only get that from DB
      - Payments
      - Balance
  - Payment Instructions:
      - 40% required amount # we have that and we can even set it dynamicaly
      - Bank account name + number
      - Deposit amount (40% of fees)
      - Deposited by (student ID + name)   # just info must be deposited by...
      - Note about processing time
      - Dean signature line (static)

  What we already have in the system

  - Student: student_id, long_name, physical_address, class_level
  - Major: student.curriculum
  - Academic semester: invoice.semester (academic year + number)
  - Course lines: invoice.curriculum_course.course.short_code, course.number,
    course.title
  - Credit hours: curriculum_course.credit_hours.code
  - Tuition rate: TUITION_RATE_PER_CREDIT (in CurriculumCourse.tuition_for)
  - Invoice totals: invoice.initial_amount_due, invoice.balance
  - Payments: Payment rows tied to Invoice



  - Department: Student doesn’t have a direct department; curriculum doesn’t lin    to a department. We’ll need a rule (e.g., derive from course department or add    to curriculum).
  - For now we will stick to the model even so some course my include extra fee, thos will be treated on the side. so a course cost is tuition_rate_per_credoit * credit_hours
  - Fee breakdown: here the fee mixes course fee (eg. lab fee, pe fee) with semester fee (registration/library fee)
  - Arrears: should be treated as balance not tied to the current semester
  - Payment instructions / bank account / dean line: Not stored. keep as stati
    settings.


**  Template editing by non‑technical users (proposal)

  - Store a template in DB (e.g., InvoiceTemplate model with html, css) and allow
    edits via a WYSIWYG:
      - Options: django‑ckeditor, django‑tinymce, or a lightweight HTML editor in
	admin.
  - Use placeholder tokens like {{ student_name }} / {{ total_due }} and a preview    action.
  - Keep the locked layout CSS separate so users only edit content blocks.

  Test plan (DB + UX)

  - DB: snapshot creation persists correct payload, total_due, and line items.
  - DB: creating snapshot for empty invoices still writes a valid snapshot.
  - UX/BDD: clicking “Download PDF” returns a PDF, opens correctly, and layout
    matches the template.


* <2026-01-30 ven. 11:49>  Invoice
   :PROPERTIES:
   :ID:       bc93afbf-000f-47eb-bece-93aa309d0dca
   :END:


-   create the invoice_snapshot model to be printed in pdf
-   add  helper functions in finance/utils where we have something similare for payment
-    On print invoice, we build the snapshot and render it (push to download)
-    Let's use weasyprint
-    Propose a template and discuss how a non technical use could edit it ( a wsiwg editor?)
  - Key layout tips:
      - Use a two‑column grid for the student info block.
      - Use a strict table for course lines (fixed column widths).
      - Use a second table for fees/summary with a right‑aligned totals column.
      - Lock fonts, sizes, and spacing with CSS to keep layout stable.
	( see data file with attached id for on layout precision )

-    After the invoice_snapshot creation:
-    plan some test, db side and also ux bdd side


* <2026-01-27 Tue 17:08>  Fee and Payments

  - need to find a clean way to import default course fix fee.

* <2026-01-23 Fri 23:01> Strange mergin and not faculty anymore ?
- Need to adress the fee structure for courses.
  - Too long to load, investigate

- [X] in student/payment/receipt
  - [X] compute  balance sum beside the paid sum
- [ ] in student/dashboard remove the Payment receipt button.
  - [ ] it is already in the menu under payment statement.
  - [ ] rename the payment statement to payment receipt in the menu



* <2026-01-23 ven. 12:49>
**  People: Merge + Admin Mixins

 - app/people/services/merge_people.py — confirm M2M merge behavior when a user is not persisted; decide if skip is
   correct.
 - app/people/services/merge_people.py — handle “source user already deleted by profile merge” case safely.
 - app/people/services/merge_people.py — verify “merge_people already merged groups/permissions; just delete
   source.”
 - app/people/admin/mixins.py — review defaulting to oldest record for target selection.
 - app/people/admin/mixins.py — review defaulting older record values for sparse fields.
 - app/people/admin/mixins.py — confirm unbound method action wiring is still needed.

**  People: Merge + Duplicate Handling

 - app/people/admin/mixins.py — confirm freeing unique values on sources is safe.
 - app/people/admin/mixins.py — resolve mixin get_queryset typing note.
 - app/people/admin/mixins.py — validate per‑request duplicate metrics caching.
 - app/people/admin/mixins.py — address “ambiguous duplicates” handling gap.
 - app/people/admin/widgets.py — reconsider cached get_or_create pattern usefulness.
 - app/people/admin/widgets.py — ensure cache path doesn’t prevent get calls.

**  People: Ensures + Models + Admin Resources

 - app/people/ensures.py — implement student lookup by student_id (primary key path).
 - app/people/ensures.py — use model mk_username consistently.
 - app/people/models/staffs.py — design organogram support for divisions/departments.
 - app/people/models/student.py — add computed level based on credits completed.
 - app/people/models/student.py — resolve prerequisite logic before uncommenting path.
 - app/people/models/object_manager.py — clarify assumptions in manager logic.

**  People: Admin Resources Mapping

 - app/people/models/object_manager.py — prevent duplicate usernames on import.
 - app/people/admin/resources.py — document or fix widgets column mapping.
 - app/people/admin/resources.py — block new major/curriculum creation during import.
 - app/people/admin/resources.py — log mismatched rows for manual handling.
 - app/people/admin/resources.py — add fuzzy curriculum match during import.
 - app/people/admin/resources_mapping.py — document AccountID origin constraints.

**  People: Admin Core

 - app/people/admin/core.py — consider moving staff_bio into FACULTY_FIELDS.
 - app/people/admin/core.py — validate the RoleAssignmentAdmin autocomplete note.

**  Timetable: Admin + Models

 - app/timetable/admin/session_widgets.py — ensure section id is available to create secsession.
 - app/timetable/admin/section_registers.py — handle/propagate error messaging.
 - app/timetable/admin/section_registers.py — clarify registrar role handling.
 - app/timetable/admin/filters.py — justify or change use_pk_exact usage.
 - app/timetable/admin/core_registers.py — confirm open semester uniqueness handling.
 - app/timetable/models/schedule.py — enforce end_time > start_time validation.

**  Timetable: Models + Ensures

 - app/timetable/models/semester.py — clarify need for REGISTRATION_OPEN_CODES set/dict.
 - app/timetable/ensures.py — decide if _created branch is needed.

**  Registry: Admin + Imports + Models

 - app/registry/admin/core.py — convert semester filter to autocomplete and limit to used semesters.
 - app/registry/management/commands/import_grades.py — validate assumption that students exist.
 - app/registry/management/commands/import_grades.py — ensure_student should resolve by student_id.
 - app/registry/models/document.py — confirm classmethods on abstract model viability.

**  Website: Student Dashboard

 - app/website/views/student_dashboard.py — review “blocked/cooldown shouldn’t be selectable” logic.
 - app/website/views/student_dashboard.py — confirm semester GPA grouping note is still needed.

 Finance + Tests

 - app/finance/admin/core.py — handle superusers without staff profiles.
 - app/finance/models/payment.py — confirm status_id is cleared before invoice update logic.
 - tests/finance/fixture.py — review finance fixtures after model changes.
 - tests/finance/test_utils.py — validate need for dummy dataclasses.

 Academics

 - app/academics/admin/core.py — add current‑semester student list for courses.
 - app/academics/admin/core.py — explain/clarify the “djangonic logic” comment.
 - app/academics/models/course.py — factor alternate‑case logic with curriculum.
 - app/academics/models/course.py — define max course number generation policy.
 - app/academics/models/curriculum.py — preserve differing info on fuzzy match.
 - app/academics/models/curriculum.py — surface warning to users on fuzzy match.
 - app/academics/models/curriculum.py — decide if using _id fields is necessary.

**  Scripts

 - scripts/merge_courses_by_short_code.sql — document COALESCE meaning.
 - scripts/merge_courses_by_short_code.sql — clarify duplication remark.

 If you want, I can turn this into a scoped execution order (P0/P1) and start with the student dashboard UX first,
 or the registrar grades dashboard first.


* <2026-01-05 lun. 07:33>  Runing a sql cmd
docker-compose -f docker-compose-preprod.yml exec -T db psql -U tusis_preprod -d tusis_preprod -c "SELECT version();"


* <2026-01-06 mar. 18:00> Notes on seed_full_student_grades.py
 Target reader: intermediate Python/pandas engineer; comfortable with typing, argparse, set ops.
 Typical US comp for that level: ~$110k–$150k base; higher with equity/bonus in SF/NYC/Seattle, closer to ~$100k–$120k in lower-cost areas.
 To increase abstraction difficulty: introduce pipeline composition/IoC for loaders+savers, plugin registry for scoring strategies, structured logging/metrics with failure policies, config-driven IO/validation, and immutable data models with strict mypy coverage.
